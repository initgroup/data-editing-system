# 퀵 에디팅 반복 오류 원인과 재발 방지

기록일: 2026-09-23. 이번 수정 대상은 혼합형 퀵 에디팅 5단계의 `Default Python API resource MIXED_XAI_PROFILE is not registered or active.` 오류입니다.

## 이번 오류의 원인

1. 혼합형 FLOW를 발굴·탐지 2단계에서 기초통계·관계 진단을 포함한 4단계로 늘리면서 `MIXED_XAI_PROFILE`, `MIXED_XAI_RELATION`을 필수 JOB으로 추가했습니다.
2. 실행 함수, FLOW 계약, 프리셋 파일은 추가했지만 기존 설치 DB의 M90002 등록은 자동으로 바뀌지 않습니다. 배포 프리셋에 있다는 사실과 DB에 등록됐다는 사실을 혼동했습니다.
3. `build_web_api_job_request`는 활성 Python 등록 목록에 없는 모델을 무조건 오류로 처리했습니다. 내장 함수가 이미 있어도 퀵 에디팅이 모델 준비에서 중단됐습니다. 이 목록은 `USE_YN='Y'`만 조회하므로 미등록과 비활성도 구별하지 못했습니다.
4. 기존 4단계 시나리오 테스트는 `ensure_default_job` 전체를 mock으로 대체했습니다. 그래프 구성은 검증했지만 실제 프리셋 → 모델 준비 → JOB 저장 → 실행 연결은 검증하지 못했습니다.

파일 내용이나 사용자 조작이 원인은 아닙니다. 기존 설치 환경으로 업그레이드할 때의 연결 조건을 구현과 검증에서 빠뜨린 문제입니다.

화면 메시지로 확인되는 직접 원인은 활성 등록을 찾지 못했다는 것입니다. 운영 DB를 직접 조회하지 않았으므로 해당 DB에서 실제 미등록인지 비활성인지는 확정하지 않았습니다. 수정은 두 상태를 분리해 처리합니다.

## 반복된 이유

기능 단위의 코드·화면 검증을 실제 사용 경로 전체의 검증으로 간주한 것이 공통 원인입니다. 지금까지 제보된 오류는 다음 경계에 집중됐습니다.

| 경계 | 드러난 문제 | 필요한 검증 |
|---|---|---|
| 결과 의미 | 이상 판정 설명을 실제 컬럼의 IF–THEN 규칙으로 취급 | 결과 컬럼·기대값·실제값과 `IF AND NOT THEN` 행을 함께 확인 |
| API 데이터 형식 | JSON 문자열과 이미 변환된 객체 구분 누락 | 문자열·dict·LOB 입력 및 저장 후 재조회 |
| SQL 조회 범위 | 과거 실행 조회에서 잘못된 `HF.FLOW_TYPE` 참조 | 설치 DDL 기반 컬럼·alias 검증과 기존 실행 이력 조회 |
| 수동 DB 업그레이드 | 새 결과 컬럼을 요구하는 코드와 기존 설치 상태의 차이 | 누락 컬럼 명시, 수동 설치 파일의 순서·재실행 가능성 확인 |
| 모델 등록 | 새 FLOW 단계와 기존 등록 목록의 차이 | 전부 미등록·일부 등록·비활성·기존 사용자 설정으로 실제 준비 함수 실행 |

위 표는 사용자 제보와 저장소의 수정 이력을 정리한 것입니다. 이번에 운영 DB의 과거 오류를 모두 재현했다는 뜻은 아닙니다.

## 이번 수정

- 활성 M90002 등록이 있으면 기존 설정을 그대로 사용합니다.
- 서버가 지정한 혼합형 4개 모델만, 실제 미등록일 때 배포 프리셋으로 해당 시나리오의 JOB을 준비합니다. `EXEC_RESOURCE_ID`는 NULL이고 메서드·입력값·출력 계약은 JOB에 보존합니다.
- 미등록 확인은 비활성·다른 언어의 등록까지 조회합니다. 그런 등록이 있으면 명확한 오류를 반환하고 관리자 설정을 대체하지 않습니다.
- 내장 프리셋의 내부 URL·POST·결과 테이블·필수 입력을 검증하며 인증 정보나 OUTPUT 매핑을 입력 파라미터에 섞지 않습니다.
- 전역 모델 등록을 쓰거나 DB 오브젝트를 생성하지 않습니다. 기존 프로젝트/시나리오 접근 검사와 트랜잭션을 그대로 사용합니다.
- 저장된 내장 JOB을 상세 화면에서 열고 저장할 때 모델명·spec·입력값이 사라지지 않도록 보완합니다.
- 실패 단계 재시도는 기존 업로드·대상 테이블·이미 저장된 JOB을 재사용합니다. 기존 JOB의 사용자 파라미터와 과거 2단계 실행은 덮어쓰지 않습니다.
- 일부 모델만 등록된 환경의 실제 FLOW 생성 테스트에서, 등록 이름을 별칭으로 사용하면 실행 메서드가 정상이어도 연결 포트를 찾지 못하는 추가 결함을 발견했습니다. 등록 이름에 계약이 없으면 실행 메서드의 계약으로 연결하도록 서버와 FLOW 화면을 함께 수정합니다. 이름과 사용자 설정은 유지합니다.

현재 실행부는 저장된 JOB의 설정을 사용합니다. M90002에서 사용 여부를 바꾸는 것을 이미 저장된 모든 JOB의 실행 취소 기능으로 해석하지 않습니다.

## 다음 변경의 완료 기준

1. 기능에 필요한 실행 코드, 모델 프리셋, FLOW 계약, 결과 테이블, 조회 SQL, 퀵/상세 화면을 한 경로로 추적합니다.
2. 등록이 갖춰진 신규 환경뿐 아니라 기존 환경을 검사합니다. 최소 사례는 전부 미등록, 새 모델만 미등록, 비활성, 사용자 기본값 수정, 기존 JOB 재사용입니다.
3. 모델 준비 핵심 함수를 실제로 실행합니다. DB 연결·외부 실행 경계만 대체하고 저장된 파라미터로 내부 실행 함수까지 연결되는지 확인합니다.
4. 실패 후 재시도와 트랜잭션 rollback을 검사합니다. 중복 JOB/FLOW 및 불완전한 저장이 없어야 합니다.
5. 결과의 의미를 확인합니다. 목표 컬럼, 기대값, 실제값, 위반 이유가 같은 원본 행을 설명해야 합니다. 수식·범위처럼 의미가 다른 지표는 정확하게 구분합니다.
6. SQL은 설치 DDL과 함께 검사하고 기존 실행 이력을 포함합니다. 오프라인 SQL 검사는 Oracle PL/SQL 컴파일·실행 검증을 대신하지 않습니다.
7. 실패했던 사례를 회귀 테스트로 남기고 통과 여부와 미검증 범위를 보고합니다. 문서 기록만으로 재발 방지가 완료됐다고 판단하지 않습니다.

## 반복 실행 가능한 검증

프로젝트 루트에서 실행합니다. 백엔드 검사기는 테스트 수집 전에 `.env` 로드와 실제 Oracle·네트워크 연결을 차단합니다. 테스트용 메모리 DB와 mock을 사용하며 운영 DDL을 실행하지 않습니다.

```powershell
.\venv\Scripts\python.exe scripts/test_offline.py
.\venv\Scripts\python.exe -m compileall -q main.py backend
node --test quick-edit/tests/*.test.js
```

이번 모델 준비 회귀만 실행하려면:

```powershell
.\venv\Scripts\python.exe scripts/test_offline.py backend.tests.test_mixed_xai_builtin_provisioning backend.tests.test_mixed_xai_scenario
```

이번 작업의 전체 검사 결과는 백엔드 317개, 프런트 51개 통과입니다. Python 컴파일 검사와 `git diff --check`도 통과했습니다. 모델 준비 회귀 13개에는 일부 모델만 등록된 업그레이드, 실제 FLOW 연결, 접근 거부, 후속 단계 실패 시 롤백 검증이 포함됩니다.

## 적용 방법과 한계

앱 소스, `frontend/config/M90002.python-api-presets.json`, `database/MCOM_DATA_WORK.sql`을 함께 배포하고 서버를 재시작합니다. 브라우저 새로고침 후 **실패 단계부터 다시 시도**합니다. `MCOM_DATA_WORK.sql`은 서버 조회문 모음이므로 Oracle에서 수동 설치하지 않습니다. 이번 오류 수정에는 새 DDL이 없습니다.

실제 운영 Target DB의 등록 상태·설치 상태와 48만 행 데이터의 전체 실행은 오프라인 테스트만으로 확인할 수 없습니다. 이 한계를 성공으로 보고하지 않습니다.

## 후속 장애: 혼합형에서 연속형 결과가 보이지 않음

두 개의 독립적인 원인을 코드에서 확인했습니다.

1. 퀵 에디팅은 혼합형 여부만 보고 연속형 탭을 숨겼고, `FORMULA` 규칙도 모두 IF–THEN 목록에 넣었습니다. 상세 분석도 공통 패턴 목록에 합쳐 보여 기존 시나리오와 비교하기 어려웠습니다.
2. 실제 파일 업로드는 데이터 컬럼을 `VARCHAR2(4000)`으로 생성하는데, 연속형 발굴은 물리 `NUMBER` 계열만 분석했습니다. 업로드의 유일한 숫자형 `FILE_ROW_NO`는 식별자여서 제외되므로 숫자 내용이 있어도 수식 대상이 사라졌습니다. 같은 물리형 의존이 기초통계·관계 진단에도 있었습니다.

이전 파이프라인 테스트는 합성 표의 X/Y를 `NUMBER`로 만들었기 때문에 실제 업로드 저장 형식과의 차이를 놓쳤습니다. 수식이 IF–THEN 카드에 표시되는 테스트만으로 연속형 화면 지원이 완료됐다고 판단한 것도 잘못입니다.

보완 사항:

- 실제 업로드와 같은 `VARCHAR2` 메타데이터·문자열 값으로 발굴부터 저장, 탐지, 결과 조회까지 회귀 검사합니다.
- 숫자 문자열은 엄격한 공통 변환기로 처리하고 AST에 `numericText: true`를 보존합니다. 원본 테이블 자료형과 기존 값 규칙은 변경하지 않습니다.
- 학습의 숫자 해석을 Oracle SQL, 화면의 실시간 조회 SQL, 최종 에디팅 검증에도 적용합니다. 세션 NLS에 맡기는 암묵적 숫자 변환을 사용하지 않습니다.
- 잘못된 결과 문자열이 있어도 위반 결과 조회가 숫자 변환 예외로 중단되지 않도록 합니다. 실제 원문은 유지하고 계산할 수 없는 잔차는 빈 값으로 표시합니다.
- 두 화면에 연속형 분류와 0건 원인을 노출하고, 과거 실행의 진단 미저장과 새 실행의 검증 기준 미달을 구분합니다.
- 규칙별 신뢰도는 있는데 KPI 평균이 없던 결과 요약 누락도 수정합니다. 수식에 해당하지 않는 향상도는 0으로 꾸미지 않고 빈 값으로 유지합니다.

앞으로 업로드 관련 분석 변경은 물리 숫자형 예제만으로 검증을 완료하지 않습니다. 숫자 문자열, 코드, 결측, 형식 오류, 별도 검증, 기존 실행 이력, 양쪽 화면에서의 분류와 0건 표시까지 확인합니다. 기존 실행에 수식이 저장되지 않았다면 분석 코드를 고쳐도 새 규칙 발굴 실행이 필요합니다.

### 연속형 보완 검증과 적용

백엔드 전체 335개 회귀 검사가 통과했습니다. 실제 업로드처럼 모든 데이터 컬럼이 `VARCHAR2`인 표에서 수식 발굴 → 공통 테이블 저장 → 생성 SQL로 위반 탐지 → 분석 결과 조회를 검사했습니다. 숫자 입력과 잘못된 결과 문자열은 실제 원문을 보존한 위반으로 조회되고, 선행 0 코드와 변환 불가 입력은 수식 적용 대상에서 제외됩니다. 기존 JOB에 새 연속형 옵션이 없어도 기본 분석을 수행하며, 명시적인 비활성 옵션은 유지합니다.

프런트 전체 55개 검사가 통과했습니다. 퀵 에디팅과 M04002에서 값·구간/수식 분류, 요약·필터·내보내기·위반 조회를 확인했습니다. 한국어·영어 브라우저 검사에는 제보와 같은 값 규칙 24개·수식 0개 화면과 실제 수식의 기대값·실제값·허용범위·잔차 조회가 포함됩니다. 평균이 없는 경우 0%로 표시하지 않으며, 연속형 탭은 0건이어도 사유와 함께 표시합니다. 이후 표시 번역을 보완하고 관련 2개 브라우저·통계 검사를 다시 통과했습니다.

Python 컴파일 검사와 `git diff --check`도 통과했습니다. Oracle 숫자 변환 SQL은 오프라인 어댑터로 검증했으며 실제 운영 Oracle의 실행·성능 검증을 대신하지 않습니다. 합성 데이터에서 검증한 규칙 수를 사용자 데이터의 예상 규칙 수로 안내하지 않습니다.

백엔드·프런트·`database/MCOM_EDIT_WORK.sql`을 함께 반영하고 서버를 재시작합니다. 이 SQL 파일은 서버가 로드하는 조회문이며 수동 설치용 DDL이 아닙니다. 이번 연속형 수정에는 새 모델 등록이나 DB 오브젝트 생성이 없습니다. 브라우저를 새로고침하면 이미 저장된 수식은 연속형 탭에서 확인할 수 있습니다. 과거 실행에 수식이 없었다면 새 FLOW 실행으로 다시 발굴해야 합니다.

## 후속 분석: 단순 합계·차이 수식 누락

연속형 탭이 표시되고 수식이 생성된다는 사실만으로 `A = B + C` 같은 구성항 관계를 찾는다고 판단할 수 없습니다. 기존 후보 생성에는 다음 제한이 있었습니다.

- 목표와의 단변수 상관이 0.1 미만인 입력은 회귀 후보에서 제외했습니다. 큰 금액 B와 작은 금액 C의 합에서는 C가 빠질 수 있고, 두 항이 상쇄되는 관계도 놓칩니다.
- 상관 순위 앞부분만 묶었습니다. 월 소득·연 소득처럼 같은 정보를 가진 변수가 앞자리를 차지하면 필요한 다른 구성항이 후보에 들어가지 못했습니다.
- 허용 오차 이내 비율을 먼저 비교해, 작은 구성항을 상수와 넓은 오차로 대신한 식이 더 정확한 합계보다 우선될 수 있었습니다.
- 퀵 에디팅 카드의 한 줄 말줄임이 THEN 뒤쪽을 감췄습니다. 회귀 계수의 수치 오차도 식에 그대로 표시되어 단순 관계를 이해하기 어려웠습니다.

독립 합성 재현에서는 `NET = YEAR - TAX`에 월 소득·연 소득 대리변수를 함께 넣었을 때 `NET ≈ 0.9999157135 × YEAR - 24.315269 ± 45.704264`만 선택됐습니다. 이는 사용자 원본 데이터를 분석한 결과가 아니라, 코드의 후보 누락과 넓은 오차를 재현한 사례입니다.

개선 후 같은 1,800행 숫자 문자열 자료에서 `NET ≈ YEAR - TAX ± 3.21124431e-11`를 발굴했고, 넣어 둔 오류 18행만 위반으로 탐지했습니다. 새 식은 `SUM_DIFFERENCE` 방식으로 저장되어 양쪽 분석 화면에서 구분됩니다. 이 예의 작은 허용 오차는 수치 계산 여유이며 모든 업무 데이터에 같은 오차를 적용한다는 뜻은 아닙니다.

큰 금액의 작은 구성항도 따로 검사했습니다. `B`의 규모가 100억이고 `C`가 1 안팎인 자료에서, 목표 IQR에 고정 비율을 곱한 동급·오차 기준은 C를 없애는 추가 원인이었습니다. 수식 항의 실제 규모와 부동소수점 정밀도에 따른 오차 하한으로 대체했습니다. 큰 양수·음수가 상쇄되는 1,000행의 숫자 문자열을 저장된 수식과 Decimal 기준으로 다시 계산해, 정상 행의 변환 오차는 허용하면서 실제 추가 오차·결측·잘못된 값은 위반으로 남는지 확인했습니다.

이 유형의 완료 기준은 수식 개수가 늘어나는 것이 아닙니다. 크기가 다른 구성항, 높은 상관의 대리변수, 상쇄 관계, 숫자 문자열, 일부 위반 행을 포함한 자료에서 실제 구성항이 저장된 AST에 들어가야 합니다. 계수 단순화는 화면에서만 반올림하지 않고 학습에서 검증한 식 자체를 저장하며, 그 식으로 보정·검증·SQL 탐지·최종 에디팅을 확인합니다. 무관한 자료에서는 규칙을 억지로 생성하지 않아야 합니다.

### 합·차 개선 최종 검증

- 백엔드 전체 349개, 프런트 전체 57개 회귀 검사가 통과했습니다. Python 컴파일과 JS 구문 검사, `git diff --check`도 통과했습니다.
- 실제 발굴 → AST 저장 → 생성 SQL 위반 탐지 → 조회 → 최종 에디팅을 숫자 문자열 자료로 확인했습니다. 새 테스트는 작은 구성항·상쇄·대리변수·큰 오류·4항 합차·정수/분수 계수·적은 종류의 건수·최대 입력 수·검증 자료 불일치·Decimal 재계산을 포함합니다.
- 한국어·영어 및 폭 500px 브라우저 검사에서 수식 전체 표시, THEN 우선 표시, 발굴 방법별 필터와 상세 표 줄바꿈을 확인했습니다.
- 동일 학습 표본의 그룹 후보를 매 타깃마다 다시 계산하던 반복 작업을 캐시하고, 고유값이 5개 나오면 범주 그룹 후보 검사를 종료했습니다. 후보 순서와 품질 기준은 유지합니다.

로컬 합성 자료 측정은 아래와 같습니다. SQL 위반 탐지나 운영 Oracle 시간을 포함하지 않으며, 두 행은 실행 범위가 서로 다릅니다.

| 자료 | 측정 경로 | 최종 측정 | 결과 |
|---|---|---|---|
| 25,000행 × 20 VARCHAR 컬럼 | `analyze_pattern_rows` 전체 | 13.154초 | 48규칙, 수식 8개, 연속형 대상 12개 |
| 25,000행 × 128 NUMBER 컬럼 | `discover_continuous_rules` | 13.757초 | 수식 6개, 대상 16개, 검증 후보 137개 |

새 모듈 `backend/services/mixed_sparse_formula.py`를 포함해 백엔드와 프런트를 함께 반영하고 서버를 재시작합니다. 새 모델 등록·라이브러리 설치·DDL 실행은 필요하지 않습니다. 과거 규칙은 보존하며 개선된 수식을 얻으려면 새 FLOW 실행이 필요합니다. 실제 사용자 원본 및 운영 Oracle 실행은 이 오프라인 검사에서 검증하지 않았습니다.

탐색은 기존 설정의 최대 입력 수(기본 3, 최대 4)와 대상·규칙 수 한도를 따릅니다. IQR이 0인 대상과 4항을 넘는 관계, 임의 비선형 수식의 포괄적인 발견을 보장하지 않습니다.

## 후속 보완: 혼합형 연속형 그래프 누락

혼합형 `FORMULA` 분기는 기존 연속형 표본 조회를 건너뛰고 차트를 숨겼습니다. 저장된 위반 미리보기만으로는 정상 행과 수식의 관계를 비교할 수 없었습니다. 수식 상세 텍스트와 연속형 탭이 보인다는 검사만으로 그래프 제공 여부까지 확인할 수는 없습니다.

M04002의 규칙 카드·상세 표·위반 결과와 퀵 에디팅의 연속형 상세를 같은 `FormulaRuleChart`로 연결했습니다. 기대값–실제값 산점도, 잔차, 저장된 허용오차 경계, 정상/위반 구분과 점·행 선택을 제공합니다. 그래프를 위해 회귀식을 다시 학습하거나 허용오차를 95% CI/PI로 표시하지 않습니다.

새 조회는 서버 세션의 실행 접근권한을 확인하고 실행·대상 소유자·테이블·모델·규칙 ID를 모두 일치시킨 저장 AST만 평가합니다. 현재 원본 앞부분 최대 5,000행을 조회한 뒤 정상·위반을 나누어 최대 300점을 표시합니다. 조건 평가 전에 원본 조회 한도를 적용하며 과거 `FORMULA` 규칙에 별도 실행 요약이 없어도 조회할 수 있습니다. 원본 조회 한도와 표시 한도를 구분하고, 과거 스냅샷·전체 원본 위반율로 오해하지 않도록 안내합니다.

재발 방지 검사에는 다음을 포함합니다.

- 숫자 문자열·결측·변환 불가 결과를 저장 규칙으로 평가하고, 계산할 수 없는 값은 0으로 그리지 않습니다. 서버의 위반 판정과 잔차를 보존합니다.
- 타 사용자 실행, 다른 모델·대상·규칙, 변경된 원본 컬럼, 잘못된 조회 한도는 거부합니다. 커넥션 정리와 읽기 전용 경로를 검사합니다.
- 다수 정상 행에 드문 위반이 있어도 제한된 표본에 함께 표시하고, IF 조건 해당 행이 없을 때도 원본 한도를 넘겨 계속 스캔하지 않습니다.
- 규칙·실행·프로젝트 전환, 팝업 닫기, 화면 종료, 이력 복원 중 늦게 도착한 응답을 새 화면에 반영하지 않습니다. 오류 후 다시 조회할 수 있습니다.
- 한국어·영어 브라우저에서 양쪽 화면의 실제 SVG, 허용경계, 잔차 전환, 선택 행 입력값, 상세 표 진입을 확인합니다. 기존 시나리오 그래프도 회귀 검사합니다.

최종 검증은 백엔드 **354개**, 프런트 **68개** 통과입니다. Python 컴파일, JS 구문, `git diff --check`도 통과했습니다. SQL은 오프라인 어댑터에서 실행했고 실제 운영 Oracle의 구문·성능은 확인하지 않았습니다.

새 서비스와 `database/MIXED_PATTERN.sql`, 공통 그래프 JS/CSS 및 양쪽 화면 파일을 함께 반영하고 서버 재시작·브라우저 새로고침을 합니다. 이 SQL은 서버가 읽는 런타임 조회문입니다. 이번 그래프 추가에는 수동 DDL이나 새 모델 등록이 필요하지 않으며, 이미 저장된 수식을 보기 위해 FLOW를 다시 실행할 필요도 없습니다.

### 그래프 도구·표현의 일관성 보완

혼합형 그래프에 점·경계·행 상세만 구현하면서 기존 상세 그래프의 확대·축소·휠 설정·이동·최대화·범례 숨기기를 빠뜨렸습니다. 그래프가 나타나는지만 검사해 실제 사용 동작과 스타일 차이를 놓친 것입니다.

기존 M04002 툴바 원본 스타일을 `rule-chart-controls.css`로 추출하고 중복 규칙을 제거했습니다. 기존 툴바와 혼합형 양쪽 화면이 같은 버튼 크기·배치·선택 색·좁은 화면 배치를 사용합니다. 혼합형은 50~800% 확대·축소, 포인터 중심 휠, 휠 사용 설정, 드래그 이동, 초기화, 최대화·복원을 지원하며 범례는 그래프 아래에서 표시 여부를 전환합니다. 선택점·행은 황색으로 강조합니다.

실제값 X / 예측값 Y로 기존 그래프와 방향을 맞췄습니다. 저장 허용오차가 예측값의 함수이므로 축 이름만 바꾸지 않고 경계 좌표 전체를 전치합니다. 잔차는 기존처럼 예측값 X / 실제값−예측값 Y이며 서버의 잔차와 판정을 유지합니다. 현재 응답에 없는 수식 AST를 문자열에서 추정하여 민감도 곡선을 만들지 않습니다.

브라우저 회귀는 확대 한도·초기화, 포인터 중심 유지, 휠 해제 시 일반 스크롤, 드래그 후 클릭 억제, 범례와 키보드 순회, 최대화 Escape·포커스·원위치·스크롤 복구, destroy 후 재진입, 한국어·영어 및 좁은 화면까지 검사합니다. 범례에서 숨긴 그룹이 방향키 이동으로 다시 켜지는 문제도 수정했습니다. SVG는 실제 표시 영역에 맞춰 좌표계를 계산하여 좁은 창에서 글자가 작아지거나 큰 빈 공간이 생기는 것을 방지하고, 화면 크기 변경 감시도 종료 시 정리합니다.

이번 변경은 프런트 파일과 캐시 버전만 갱신합니다. 새 공통 CSS를 포함해 배포하고 브라우저를 새로고침하면 됩니다. 모델 등록·FLOW 재실행·DB 변경은 필요하지 않습니다.

프런트 전체 74개 검사가 통과했고, 후속 키보드·반응형 보완 뒤 관련 브라우저 11개를 다시 통과했습니다. JS 문법·diff 검사와 한국어·영어 실제 화면 검수도 완료했습니다. 운영 서버의 사용자 데이터 실행은 이번 프런트 검증에 포함하지 않았습니다.

### 기존 그래프까지 같은 결함 점검

혼합형에서 발견한 문제는 기존 Chart.js 상세 그래프와 퀵 에디팅 canvas에도 같은 항목으로 확인합니다. 신규 그래프만 통과했다고 전체 그래프 품질이 개선됐다고 판단하지 않습니다.

- 기존 M04002 휠 확대는 표시 비율만 50~800%로 제한하고 실제 축에는 계속 배율을 적용했습니다. 제한된 다음 비율에서 유효 배율을 계산하도록 수정합니다. 휠·이동 좌표는 CSS 표시 좌표를 Chart.js 내부 좌표로 변환하고 실제 차트 영역 안에서만 처리합니다.
- 기존 상세의 숨긴 표본은 키보드 순회에서 제외합니다. 표에서 행을 명시적으로 선택하면 해당 표본 그룹을 표시합니다. 범례는 키보드로도 선택 가능하며 CI·PI의 상한/하한을 함께 전환합니다.
- 최대화 중 Escape는 복원부터 수행합니다. 팝업 닫기·화면 종료 시 포인터 캡처와 문서에 등록한 팝업 이동 이벤트를 정리합니다.
- 기존 퀵 에디팅은 그리기 폭을 최소 560px로 고정해 좁은 화면에서 글자·점까지 축소했습니다. 실제 canvas CSS 폭과 DPR을 기준으로 그리고, 컨테이너 크기 변경과 숨김 후 복원도 관찰합니다. 페이지 이탈 시 관찰자·대기 타이머를 정리하고 복귀 시 다시 연결합니다.
- 퀵 에디팅 그래프의 방향키는 그래프 포커스를 유지하며 표의 해당 행만 선택·스크롤합니다. 표의 방향키는 표 포커스를 유지합니다. 표시된 점과 클릭 반경은 CSS 픽셀 기준으로 맞춥니다.
- 좁은 화면에서는 범례 줄 수와 축 숫자의 실제 텍스트 폭을 먼저 측정하여 그래프 영역과 축 제목이 겹치지 않게 배치합니다. 그래프 해석 안내도 생략하지 않고 줄바꿈합니다.

기존 CI/PI는 표본 회귀 진단, 혼합형 경계는 저장 규칙의 허용오차라는 차이는 유지합니다. 도구 동작을 맞추기 위해 분석 기준이나 판정을 바꾸지 않습니다. 실제 Chart.js·canvas를 사용하는 브라우저 회귀에서 확대 한도, CSS 배율, 키보드, 숨긴 범례, 빈 표본, 크기 변경, 페이지 복귀와 종료를 점검합니다.

최종 프런트 전체 **89개** 검사가 통과했습니다. 기존 그래프 신규 회귀 15개에는 확대 후 축 범위 밖의 점을 방향키로 선택하면 해당 점으로 이동하는 경우, 빈 표본으로 바뀌면 옛 좌표·접근성 설명을 제거하는 경우도 포함합니다. 실제 Chart.js 상세 화면과 퀵 에디팅 canvas의 데스크톱·좁은 화면을 캡처하여 범례·축 제목·눈금 배치도 확인했습니다. JS 문법과 `git diff --check`를 통과했습니다. 이번 수정은 프런트만 배포·새로고침하면 되며 DDL이나 규칙 재발굴은 필요하지 않습니다.

### 후속 점검: 그래프·그리드·결과 탭의 실제 동등성

앞선 보완은 툴바 동작에 집중해 원본 입력 컬럼이 없는 20행 페이징 표, 별도의 수식 배너, 다른 선택점·윤곽선, 사각 결과 탭을 남겼습니다. 같은 점을 선택할 수 있다는 검사만으로 기존 사용 방식과 같다고 판단한 것이 원인입니다.

- 기존·혼합형의 점/선 스타일은 `RegressionDiagnostics.chartStyle`과 `pointStyle`로 통합합니다. 툴바에 이어 수식 배너·복사 버튼도 공통 CSS로 추출하고 기존 기본/미디어 중복 규칙을 제거했습니다.
- 혼합형의 표본은 전체 300행 스크롤 그리드로 표시하고 원본 X, 실제 Y, 기대값, 잔차, 허용범위, 판정을 함께 제공합니다. 점→행/행→점, 선택 색상, 컬럼 설명·X/Y 배지, 헤더/No 고정, 키보드와 스크롤 보존까지 검증합니다.
- `pointerup` 후의 차트 클릭 억제가 곧바로 이어지는 그리드 행 클릭까지 막는 문제를 발견했습니다. 억제 범위를 점 클릭에 한정했습니다. 점을 클릭한 직후 지연 없이 다른 행을 클릭하는 회귀 검사를 유지합니다.
- 좁은 화면의 긴 음수 축 눈금이 잘리는 문제를 발견했습니다. 표시 눈금의 정밀도·지수 표기 및 실제 글꼴 측정에 따른 축 여백으로 해결합니다. 표·수식 평가·위반 판정 수치는 변경하지 않습니다.
- M04002의 family 탭과 수식 카드·위반 요약은 기존 연속형의 원본 스타일을 공유합니다. `[hidden]`을 `display:grid`가 덮어써 혼합형에 미적용 max-rules/lift 기준이 노출되는 문제도 발견해 해당 분기에서는 DOM을 생성하지 않도록 수정했습니다.

화면 검수는 기존/혼합형 × 퀵 에디팅/M04002 × 발굴/위반 결과를 함께 비교합니다. 데이터가 있는 화면뿐 아니라 빈 표본, 메타가 없는 과거 응답, 긴 수식/컬럼 설명, 한국어/영어, 좁은 화면, 최대화·복원도 포함합니다. 스타일 공통화는 판정 의미를 바꾸지 않습니다. 기존 CI/PI 진단과 혼합형 저장 허용오차를 구분하고, 거의 0인 허용경계를 눈에 띄게 하려고 임의 확대하지 않습니다.

표본 API에는 이미 검증한 입력 컬럼과 행 식별자 메타만 추가했습니다. 새 SQL·DDL·모델 등록·규칙 재발굴은 필요하지 않습니다. 백엔드와 공통 프런트 리소스를 함께 반영하고 서버 재시작·브라우저 새로고침을 적용합니다.

최종 검증: 백엔드 오프라인 전체 **355개**, 프런트 전체 **99개** 통과. Python 컴파일·변경 JS 문법·diff 공백 검사를 통과했으며, 한국어/영어 및 390px 화면의 실제 브라우저 캡처를 확인했습니다. 기존 Chart.js/canvas와 혼합형 SVG의 점 선택·경계·범례·최대화·그리드 연동을 함께 검사했습니다. 실제 운영 Oracle 또는 사용자 원본 데이터에 대한 실시간 검증은 수행하지 않았습니다.

### 재제보: 점 클릭 누락·사각 외곽·선택점 표시

서비스의 공개 정적 JS/CSS 응답을 워크스페이스 파일과 비교해 같은 파일임을 확인했습니다. 배포 누락으로 단정하지 않았습니다. 이전 검사는 DOM 점의 중심을 정확하게 클릭하는 방식이어서 사용자의 작은 클릭 오차와 손 움직임을 놓쳤습니다. 혼합형은 반지름 3px인 SVG 도형에 직접 닿은 이벤트만 선택했고, 3px 안팎 움직임도 팬으로 처리했습니다. 좌표를 점에서 8px 옮겨 실제 마우스로 클릭하는 재현 검사는 수정 전 실패했습니다.

- 세 그래프가 화면 픽셀 기준 14px의 근접 선택 기준을 공유합니다. SVG는 현재 화면 좌표로 변환해 보이는 표본만 검사하고, 기존 Chart.js도 CI/PI·기준선의 hit 결과 대신 실제 표본을 직접 검사합니다. 줌 범위 밖/범례에서 숨긴 점은 클릭 대상으로 삼지 않습니다.
- 드래그 판정은 CSS 좌표에서 6px 이상 이동한 경우로 분리합니다. 실제 드래그 뒤 합성 클릭은 억제하되 작은 손 움직임은 클릭으로 처리합니다. 화면 배율 변경 상태에서도 같은 기준을 검사합니다.
- SVG·기존 상세 Chart.js·기존 Quick canvas 모두 실제 plot의 상하좌우 테두리를 명시적으로 그립니다. 기존의 격자선이나 차트 컨테이너 테두리만으로 대체하지 않습니다.
- 혼합형은 근접 hover 설명과 선택 행/실제값/기대값/잔차 표시를 제공하며 그리드 선택 행에 상하 테두리를 그립니다. 기존 그래프에서도 선택점을 마지막에 그려 겹친 다른 점에 가려지지 않게 합니다.

수정 후 프런트 전체 **102개** 검사 통과. 추가 검사는 실제 마우스의 근접 클릭·미세 이동·화면 배율·hover·표본 선택과 사각 테두리 그리기를 포함합니다. 수정한 JS 문법과 diff 검사도 통과했습니다. 이번 변경은 프런트 리소스와 캐시 버전만 변경하며 DB/규칙 재실행이 필요하지 않습니다. 운영 원본 실행의 브라우저 세션을 직접 조작한 검증과는 구분합니다.
