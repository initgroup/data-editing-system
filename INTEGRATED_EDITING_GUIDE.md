# 통합 에디팅 4단계 운영·배포 안내

이 변경은 현재 제품의 **컬럼 유형·기초통계 → 컬럼 관계 → 규칙 발굴 → 위반 탐지** 흐름을 확대합니다. 각 단계에서 기존 Oracle 분석을 먼저 수행하고 Python 혼합형 보강을 이어서 수행합니다. 프로젝트·시나리오·JOB·FLOW·결과 분석·최종 에디팅·보고서·관리 메뉴를 그대로 사용하며 별도 제품이나 별도 실행 시스템을 요구하지 않습니다.

퀵 에디팅의 신규 작업은 프로세스 선택 없이 `UNIFIED` 4단계로 자동 실행합니다. 기본 FLOW 설계도 `UNIFIED`를 권장합니다. 기존 `LEGACY`, `MIXED_XAI` JOB/FLOW와 실행 이력은 당시 계약으로 계속 실행·조회합니다. 이전 작업을 여는 것만으로 통합 실행으로 바꾸거나 저장 설정을 덮어쓰지 않습니다. 브라우저에 예전 선택만 남아 있고 등록·설계된 작업이 없으면 신규 통합 실행으로 시작합니다. 개선된 규칙을 얻으려면 새 통합 FLOW를 실행해야 합니다.

## 단계별 실행과 저장

| 단계·메뉴 | 통합 Python API | 기존 분석 유지 | Python 보강 |
|---|---|---|---|
| 1. M03001 | `UNIFIED_EDITING_PROFILE` | `INIT$_SP_PREDICTED_TYPE`, 범주형·연속형 유형 결과 | 표본 기초통계, 숫자 문자열·변환 불가 값, 식별자·상수·빈 컬럼 진단 |
| 2. M03002 | `UNIFIED_EDITING_RELATION` | 기존 관계 행렬·네트워크와 설정된 분석 | 수치 상관, 범주형 관계, 범주형–연속형 관계, 중복·결측 진단 |
| 3. M03003 | `UNIFIED_EDITING_DISCOVER` | 기존 OML 연관규칙·연속형 발굴 | 실제 컬럼의 IF–THEN 값·범위 규칙, 합·차·선형·비율·조건부 수식 |
| 4. M03004 | `UNIFIED_EDITING_DETECT` | 기존 OML·연속형 규칙 위반 탐지 | 저장된 조건·결과 AST를 전체 원본에 적용해 위반 탐지 |

유형 구분과 기존 관계 분석은 계속 사용합니다. 혼합형 보강은 물리 메타데이터와 제한된 원본 표본을 별도로 검토하므로 앞 단계의 범주 판정이나 낮은 단변수 상관만으로 수식 후보를 모두 차단하지 않습니다. 보강 진단으로 원본 컬럼 자료형이나 사용자가 저장한 유형 설정을 변경하지 않습니다.

두 분석은 같은 대상·실행 종류·실행 ID에 저장하면서 모델과 규칙 출처를 구분합니다. 통합 단계의 실행 근거는 기존 `INIT$_TB_XAI_RUN.SUMMARY_JSON` 안의 `integratedEditing.stages`에 보관합니다. 혼합형의 기존 `profile`, `relationships`, 규칙 AST와 검증 지표도 유지합니다. 통합 발굴에서 자동 이름이 필요한 OML 모델은 실행별 이름을 사용합니다.

## 결과 확인과 의미

퀵 에디팅은 **홈 바로 아래 퀵 에디팅(M00001)** 메뉴와 상단 바로가기에서 메인 화면의 탭으로 엽니다. 같은 탭을 다시 선택하면 진행 상태를 이어서 확인하며, 결과상세는 같은 메인창의 M04002로 이동합니다. 기존 `/quick-edit/` 독립 주소는 호환 경로로 유지합니다.

브라우저 작업 상태는 서버 세션에서 확인한 사용자·대상 DB가 일치할 때만 복원합니다. 사용자 범위가 없는 예전 브라우저 초안은 복원하지 않으며, 저장된 JOB·FLOW·실행 이력은 삭제하지 않습니다. 실행 중 메뉴 전환은 가능하고, 탭 닫기·새로고침·대상 DB 변경은 진행 중인 요청과 미저장 컬럼 편집 상태를 확인합니다.

같은 4단계 안에서 **OML 범주형·연속형 분석**과 **혼합형 값·범위·수식 보강 분석**을 유지합니다. 최종 규칙 목록은 아래 표현 유형으로 묶고, 발굴 방법 상세에서 원래 분석·그래프·내보내기·최종 에디팅 경로를 사용합니다.

퀵과 M04002의 최종 실제 데이터 규칙은 **조건규칙(IF–THEN) / 수식규칙**으로 묶습니다. 기존 OML 연관규칙과 혼합형 값·범위 규칙은 조건규칙에, 기존 Symbolic과 혼합형 수식은 수식규칙에 표시합니다. 조건부 수식도 원래 적용 조건·저장된 식·허용오차를 유지합니다. 이는 결과의 표현 유형을 통합한 것이며 분석 엔진이나 저장된 위반 판정을 바꾸는 작업이 아닙니다.

발굴 방법은 규칙별로 표시하고 필요할 때 필터링합니다. 원래 모델의 점수, 신뢰도·향상도, 허용오차 충족률·검증 R²는 의미가 달라 하나의 공통 점수로 평균내지 않습니다. 모델·실행·대상·규칙 ID를 함께 보존하므로 서로 다른 엔진의 같은 규칙 ID가 상세 조회에서 충돌하지 않습니다. 의미가 비슷한 규칙도 검증 범위가 다를 수 있어 자동 중복 제거하지 않습니다.

과거 `ISOLATION_FOREST_SURROGATE`의 이상 후보 설명은 실제 값의 IF–THEN 위반 규칙이 아니므로 두 유형에 강제로 넣지 않습니다. **과거 이상 후보 설명**에서 별도로 확인합니다. 유형·관계·특성 선정과 같은 중간 단계의 진단 화면도 유지합니다.

규칙 목록은 기본 20개씩 서버에서 조회합니다. 전체 건수와 현재 페이지를 구분하고, 선택한 규칙의 저장 위반 미리보기·그래프는 필요할 때 조회합니다. 퀵의 기초통계·컬럼 유형·상세 분석 진단은 별도 불러오기 동작으로 분리하여 느린 전체 통계가 규칙 목록을 가리지 않게 합니다. 저장 미리보기는 실행 설정의 저장 상한을 따르며 원본 전체를 다시 탐지한 결과가 아닙니다.

규칙 목록은 조건 개수와 0건 제외로 필터링합니다. 서버에서 전체 선택 범위를 먼저 정렬한 뒤 페이지를 나눕니다. 위반이 있는 규칙을 건수 내림차순으로, 미기록을 그 다음으로, 0건을 마지막으로 표시합니다. 탐지 전체 건수가 없으면 저장된 위반 행 수를 사용하고 그 기준을 표시합니다. 이 순서는 확정 오류 확률 순위가 아닙니다. 상세 상단의 규칙 목록 버튼은 이전 필터·페이지·선택 규칙·스크롤 위치를 복원합니다. 긴 규칙 ID와 모델·대상은 실행 식별 정보를 펼쳐 확인합니다.

신뢰도 70%가 같은 적용 대상의 탐지 후 지표라면 조건에 해당하는 행 중 약 30%는 위반입니다. 학습·검증 표본의 신뢰도와 탐지 대상의 위반율은 다를 수 있습니다. 혼합형 탐지는 적용 대상 건수와 충족 건수의 차이를 전체 위반 건수로 저장하지만, 공유 미리보기 한도로 특정 규칙의 저장 행은 0건일 수 있습니다. 화면에서 탐지 건수·저장 건수·미기록·집계 불일치를 구분하며, 저장된 행이 없다는 이유로 원본에 위반이 없다고 판단하지 않습니다.

수식 0건은 실행 실패와 같지 않습니다. 저장된 진단으로 미평가 대상, 수치 적격성, LASSO 진입 기준, 허용오차·검증 기준 탈락을 구분하며 진단이 없는 과거 실행에는 그 한계를 표시합니다. 일부 출처를 조회하지 못한 경우에는 부분 결과임을 표시합니다.

- 두 분석에서 같은 행이나 관계를 찾을 수 있습니다. 분석별 규칙 수·위반 건수를 단순 합산한 값을 고유 오류 행 수나 오류 셀 수로 표시하지 않습니다.
- 규칙×행 위반, 고유 위반 행, 저장 미리보기 건수는 다른 지표입니다. 현재 결과는 검토 근거이며 확정 오류 확률이 아닙니다.
- `총액 = 공급가액 + 세액` 위반만으로 총액 셀이 잘못됐다고 확정할 수 없습니다. 여러 관계를 결합한 오류 셀 위치 추정과 자동 수정값 결정은 이번 구현에 포함되지 않습니다.
- 수식의 기대값과 허용오차는 저장된 AST로 계산합니다. 화면 설명만 고치거나 조회할 때 다시 회귀 학습하지 않습니다.
- 정상·오류 판정과 원본 반영은 기존 최종 에디팅의 검토·수정·재검증 흐름을 따릅니다.

## 보강된 분석 품질

### 숫자 문자열과 원본 오타

수식 발굴은 계수 학습 구간에서 비어 있지 않은 값의 **98% 이상**이 엄격한 숫자 문법을 만족하고 최소 유효 건수(기본 30건)를 충족하면 숫자 문자열 컬럼으로 분석합니다. 기초통계의 표본 진단은 별도의 소규모 관측 기준을 사용하며 수식 학습 적격 판정과 구분합니다.

부호·소수·지수 표기를 허용하지만 쉼표, 통화기호, 임의 문자 제거로 숫자를 만들어내지 않습니다. `001`처럼 선행 0이 있는 코드는 계속 보호합니다. 일부 오타 때문에 전체 컬럼의 분석을 포기하지 않되, 오타 자체는 숫자로 인정하거나 0으로 대체하지 않습니다.

계수 학습·보정·검증 구간의 변환 불가 건수를 따로 기록합니다. 유효하지 않은 입력값은 수식 적용 범위에서 제외하고, 유효한 입력에 대한 결과값이 결측·변환 불가이면 위반입니다. Python 학습, Oracle SQL, 그래프·실시간 조회, 최종 에디팅은 같은 `numericText` AST 정책을 사용합니다.

### 전체 관계와 다른 정상 소수 집단

전체 수식이 통과해도 소수 집단에 다른 안정적인 수식이 있는지 제한된 범위에서 추가 탐색합니다. 학습 표본에서 2~4개 범주가 있는 컬럼을 대상으로 그룹 후보를 구성하며 같은 학습 행 집합을 만드는 중복 후보는 제외합니다. 이미 전체 수식으로 충분히 설명되는 집단도 반복 탐색하지 않습니다.

대안 수식은 기존 학습·검증 기준을 통과하고 해당 집단에서 전체 수식보다 허용오차 이내 비율이 학습과 선택 검증 모두에서 최소 5%p 높아야 합니다. 채택하면 전체 수식의 IF 조건에서 그 집단을 제외하고, 나머지 적용 범위의 지표를 다시 검사합니다. 전체 수식의 계수와 허용오차를 다시 학습하지 않습니다.

예를 들어 `A 집단: Y≈2X+3`, `B 집단: Y≈5X−10` 관계가 각각 검증되면 B 집단을 전체 수식의 위반으로 중복 지적하지 않습니다. 이는 설명용 예시이며 실제 데이터의 발견을 보장하지 않습니다. 그룹·대상 컬럼·수식 수 제한과 최소 표본 요건이 있으며, 임의의 모든 집단·비선형 관계를 탐색하지는 않습니다.

### 범주형·연속형 관계 진단

- 연속형 쌍: 유효한 짝의 Pearson 상관과 자료 존재율.
- 범주형 쌍: 편향 보정 Cramér's V와 희소 기대빈도 비율.
- 범주형–연속형 쌍: 상관비 η와 η², 집단 수와 최소 집단 건수.

기본 진단 한도는 수치 64개 컬럼, 범주형 32개 컬럼·컬럼당 32개 범주, 범주형/혼합형 각 최대 256개 후보 쌍입니다. 결과는 각 종류별 최대 100개 쌍을 보관하고 제외·절단 여부를 함께 기록합니다. 이 값들은 표본의 관계 진단이며 인과관계·에디팅 규칙·오류 확률이 아닙니다.

## 표본 방식과 자원 관리

새 통합 프리셋의 혼합형 보강은 `P_SAMPLING_STRATEGY=HASH`를 사용합니다. 이전 JOB에서 표본 전략이 지정되지 않은 경우에는 기존 `FIRST_ROWS` 동작을 유지합니다. 설정은 모델/JOB에서 관리하며 서버·사용자 자원 상한을 우회하지 않습니다.

| 방식 | 의미와 비용 |
|---|---|
| `FIRST_ROWS` | 원본 앞부분을 제한하여 조회합니다. 정렬된 자료 전체를 대표한다고 해석할 수 없습니다. |
| `HASH` | 전체 건수 조회 후 ROWID 해시 조건으로 후보를 읽고, 후보 전체에서 결정적인 해시 우선순위로 제한된 표본을 유지합니다. 원본 조회 순서에 따른 앞부분 편향을 줄이지만 건수 조회와 해시 필터가 전체 스캔 비용을 낼 수 있습니다. |

HASH는 층화 표본이 아니며 희귀 집단 포함을 보장하지 않습니다. 같은 seed·ROWID·원본 값에서 재현성을 기대할 수 있지만, 조회 사이의 Oracle 스냅샷을 고정하지 않습니다. 분석 중 원본 변경과 테이블 재생성·이동은 표본과 결과를 바꿀 수 있습니다. 실행 비교에 필요한 데이터 버전 고정은 별도 운영 관리가 필요합니다.

Python 보강은 최대 25,000행, 최대 128개 원본 컬럼과 약 32 MiB의 보관 표본 제한을 적용합니다. 실제 컬럼 수는 서버/사용자 자원 제한에 의해 더 줄어들 수 있습니다. 메모리 수치는 보관 행의 추정치이며 드라이버 버퍼·Python 객체·모델 전체 RSS의 상한은 아닙니다. 바이트 한도로 표본이 축소되면 대표성 제한을 표시합니다. 후보 수가 부족하면 더 적은 표본을 반환하고 이를 진단합니다.

원본 컬럼이 모델 입력 한도보다 많으면, 컬럼 묶음별 최대 512행·2 MiB의 작은 검토 표본으로 수치 적격성과 변동을 확인한 뒤 모델 입력을 선택합니다. 명시적인 대상·제외 설정을 보존하며 실제 모델의 입력·행·용량 상한은 유지합니다. 이 검토는 앞부분 표본이므로 뒤쪽이나 희귀 값을 놓칠 수 있고, 묶음별 원본 시점을 고정하지 않습니다. 선택·제외 컬럼과 추가 조회 비용은 `featureScreening` 진단에 남깁니다. 통계적 검증 기준을 낮추거나 미발굴 수식을 임의로 만들지는 않습니다.

현재 혼합형 분석에는 사용 가능한 컬럼이 최소 2개, 규칙 발굴 표본은 최소 32행 필요합니다. 실제 규칙 채택에는 학습·검증별 최소 지지 건수도 충족해야 합니다. 최소 입력 요건을 충족하지 못하면 해당 단계가 오류 사유를 반환하며, 요건을 충족해도 검증된 규칙이 없을 수 있습니다.

이 정책은 **혼합형 보강 표본**의 정책입니다. 기존 OML 분석 경로의 전체 처리 범위나 그래프의 별도 표본 조회 정책까지 바꾼다는 뜻은 아닙니다. 발굴 표본 통계와 전체 원본 탐지 건수를 구분합니다.

## 설정·실패·재시도

통합 API는 기존 분석 옵션과 혼합형 옵션을 분리합니다. 예를 들어 `P_MIN_CONFIDENCE`는 기존 분석 기준, `P_MIXED_MIN_CONFIDENCE`는 혼합형 기준입니다. `mixedOptions` 객체도 허용된 혼합형 설정만 전달할 수 있으며, 대상 소유자·테이블·실행 범위를 바꿀 수 없습니다. 통합 발굴과 탐지는 같은 저장 실행을 사용합니다.

M90002에 활성 통합 API 등록이 있으면 이를 사용합니다. 미등록 내장 통합 API는 서버의 허용 목록과 배포 프리셋 계약으로 JOB을 준비하며 전역 관리자 모델을 임의 생성하지 않습니다. 비활성·잘못된 등록은 대신 켜거나 덮어쓰지 않습니다. 기존 JOB의 사용자 설정은 보존합니다.

자동 생성된 작업은 M03001~M03004의 작업 목록에서 선택합니다. 미등록 내장 API로 만든 JOB에는 중앙 등록 ID가 없을 수 있지만, 저장된 실행 메서드·입력값·호출 계약을 기준으로 모델 선택과 파라미터가 복원됩니다. 이를 등록 누락 오류로 오해해 다른 모델을 다시 선택하거나 M90002 등록을 먼저 만들 필요는 없습니다. 모델 선택창의 저장 JOB용 내장 항목은 전역 등록이 생성됐다는 뜻이 아닙니다.

M90002의 **기본 Python API 생성**은 배포 설정의 기본 대상 중 미등록 항목만 등록합니다. 이미 등록된 항목의 파라미터·설명·사용 여부를 변경하지 않습니다. **Python API 기본 JSON 적용**은 선택한 API 편집값에 프리셋을 불러오는 동작이며, 변경 내용을 확인한 뒤 저장합니다. **초기화**는 프리셋이 연결된 항목에는 기본 JSON을 다시 적용하고 일반 저장 항목에는 저장값을 다시 불러옵니다. 저장 전에는 DB 등록에 반영되지 않으며 전체 시스템·DB 설치 초기화가 아닙니다. 중앙 등록을 저장해도 기존 JOB과 과거 실행의 저장 설정을 일괄 갱신하지 않습니다.

M03001~M03004의 DB 객체는 M90001 등록·상세 설정을, 등록 API는 M90002 호출 계약을 사용합니다. 미등록 내장 API JOB은 저장된 계약을 사용합니다. 설정 출처를 구분하고, 기본 JSON의 현재 값보다 선택한 JOB과 실행 이력의 실제 파라미터를 우선 확인합니다.

**가져오기**는 원본 JOB을 현재 작업으로 이동하는 기능이 아니라, 원본의 실행 계약·파라미터를 가진 새 미저장 초안을 만듭니다. 대상 프로젝트·시나리오·테이블과 실행 설정을 확인한 뒤 저장하고 실행합니다. 실행 유형에 따라 생성 버튼과 내용은 **PL/SQL 생성**(DB 객체), **OML SQL 생성**, **API 명세 생성**(Web API)으로 구분됩니다. 통합 4단계는 API 명세를 사용하며, 가져온 미등록 내장 JOB도 원본에 저장된 계약을 유지합니다.

FLOW에 연결할 때에는 저장된 JOB과 현재 대상·단계·입출력 계약을 함께 확인합니다. 이름이 비슷한 다른 JOB으로 노드를 재연결하거나 과거 FLOW를 자동 통합형으로 간주하지 않습니다. 필수 SAME_RUN 입력은 같은 실행의 호환 선행 결과를 연결하고 FLOW 검증 후 실행합니다.

두 구성 분석이 모두 성공해야 해당 통합 단계가 성공합니다. 기존 분석이 실패했는데 혼합형만 성공한 상태를 정상 완료로 반환하지 않습니다. 일반 결과 DML은 호출자의 트랜잭션으로 관리하고 기존 실행 실패·재시도 경로를 사용합니다.

Oracle 모델 생성 등 DDL의 암묵적 커밋까지 롤백하는 기능은 아닙니다. 운영에서 실패·재시도 시 모델과 결과 상태를 함께 확인해야 합니다. 영속 작업 큐, 서버 재시작 후 모든 작업 자동 복구, 독립 DB 스냅샷은 이번 변경에 추가되지 않았습니다.

기본 보고서 R08은 범주형 규칙, R10은 기존 Symbolic과 통합·혼합 수식을 함께 집계하고 R11도 같은 구분을 적용합니다. 수식 표본은 최대 300개이며 표시 수식의 원문을 보존합니다. 통합·혼합 수식 Confidence와 Symbolic 점수는 합산하지 않습니다. R11은 저장된 규칙×행 위반의 집계이므로 저장 표본 제한이 있는 실행에서는 전체 원본 위반 건수와 다를 수 있습니다. `RESULT_KIND`가 없는 구형 스키마에서는 기존 규칙 출처·수식 ID 계약으로 분류합니다.

## 설치·Render 배포

이번 변경은 기존 Python 의존성을 재사용합니다. **새 라이브러리, 새 DB 오브젝트, 이번 개선만을 위한 추가 ALTER DDL은 없습니다.** 시스템 DB/Target DB 연결과 계정 분리, 관리자 권한, 개인 환경설정, 서버 자원 상한, 보고서 생성은 기존 구조를 유지합니다.

다만 통합 분석은 기존 OML 분석 오브젝트와 **이미 정의된 혼합형 저장 스키마**가 모두 필요합니다. 혼합형을 설치하지 않은 고객 DB는 통합 실행 전 해당 설치를 완료해야 합니다. 스키마가 없으면 기존 결과를 변경하기 전에 실행을 중단하며 분석 도중 DDL을 자동 적용하지 않습니다.

기존 설치 상태별 적용 파일은 [MIXED_XAI_GUIDE.md](MIXED_XAI_GUIDE.md#수동-sql과-모델-등록)를 확인합니다. `database/INIT_TARGET_PATTERN.sql`, `database/INIT_TARGET_XAI.sql` 등은 설치 상태에 맞춰 관리자가 검토·적용하는 기존 설치 파일입니다. **`database/ANALYSIS_SAMPLING.sql`, `database/EDITING_RESULTS.sql`과 변경된 조회 SQL은 서버가 읽는 런타임 SQL이며 수동 설치 DDL이 아닙니다.** 서버를 재시작하고 브라우저를 새로고침하면 저장된 실행 결과에도 새 조회 방식이 적용됩니다. 조회 화면 변경만을 위해 분석을 다시 실행할 필요는 없습니다.

배포 순서:

1. 검증용 시스템/Target DB 연결에서 기존 OML 및 혼합형 설치 상태·권한을 확인합니다. 운영 데이터에 검증용 DDL이나 임의 변경을 실행하지 않습니다.
2. 백엔드, 새 런타임 SQL, 통합 API 프리셋·FLOW 계약, 프런트 리소스를 함께 배포합니다.
3. 기존 Render 빌드 명령 `bash scripts/render-build.sh`와 Start Command를 사용하고 서버를 재시작합니다. 새 SQL ID는 서버 시작 때 로드합니다.
4. 브라우저를 새로고침하고 새 통합 FLOW를 생성·실행합니다. 과거 `LEGACY`/`MIXED_XAI` 작업도 각각 열어 확인합니다.
5. 양쪽 결과의 규칙·위반·그래프·최종 에디팅, 기존 계정·환경설정·DB 연결·보고서 기능을 확인합니다.

Render의 Oracle 연결·지원 버전·권한·메모리·CPU·해시 조회 실행계획과 실제 데이터 소요시간은 배포 환경에서 별도로 검증해야 합니다. 소스의 오프라인 검사가 통과했다는 사실만으로 운영 배포 검증이나 GS 인증이 완료되지는 않습니다.

## 검증과 제품 범위

### 미등록 통합 API를 중앙 관리 항목으로 추가할 때

자동 JOB의 실행·복원을 위해 중앙 등록을 반드시 만들 필요는 없습니다. 관리자가 통합 API의 기본값을 중앙에서 관리하려는 경우 다음 절차를 사용합니다.

1. 외부 모델 등록(M90002)을 열고 **미등록 기본 Python API 생성**(마술봉) 버튼을 누릅니다. 현재 배포 기본 목록은 기존 `INTEGRATED_RELATION_CLUSTER`, `INTEGRATED_RULE_DISCOVER`, `INTEGRATED_RULE_VIOLATION_DETECT`와 통합 `UNIFIED_EDITING_PROFILE`, `UNIFIED_EDITING_RELATION`, `UNIFIED_EDITING_DISCOVER`, `UNIFIED_EDITING_DETECT`입니다. 이 중 미등록 항목만 생성하며, 이미 등록·비활성화된 항목은 보존합니다.
2. 통합 4개만 개별 등록하려면 **등록됨** 필터(등록된 API 객체만 조회)를 해제하고 트리에서 해당 `UNIFIED_EDITING_*` 프리셋을 하나씩 선택합니다. 입력 기본값·호출 계약·사용 여부를 검토한 뒤 각 항목을 **저장**합니다.
3. 이미 등록된 항목을 의도적으로 바꾸려면 해당 항목을 직접 선택해 수정·저장합니다. **기본 JSON 적용**이나 **초기화** 후에는 편집값을 확인하고 저장 여부를 결정합니다. 기본 생성 버튼으로 기존 항목을 갱신하지 않습니다.
4. 저장 후 해당 항목의 등록 상태를 확인합니다. 이후 새로 구성하는 JOB에 사용할 모델과 값을 확인하고, 기존 JOB은 저장된 모델·파라미터를 별도로 검토합니다. 중앙 등록만으로 기존 JOB·과거 실행이 변경되지는 않습니다.

이 안내는 소스에 구현된 등록 절차입니다. 실제 운영 DB 등록·설치·배포를 수행했다는 의미는 아닙니다.

`.env` 로드·실제 Oracle·네트워크 연결을 차단한 백엔드 회귀 검사는 다음처럼 실행합니다.

```powershell
.\venv\Scripts\python.exe scripts/test_offline.py
.\venv\Scripts\python.exe -m compileall main.py backend
```

주요 회귀 대상은 4단계 실제 디스패치, 양쪽 결과 보존, 모델 미등록/비활성/기존 JOB, 실패와 재시도, 서버 실행 접근권한, 원본 VARCHAR 숫자·오타·선행 0 코드, 안정적인 소수 집단, 무관 자료, AST–SQL–최종 에디팅 일치, 표본 순서·바이트 제한입니다. Oracle 전용 프로시저·문법은 오프라인 어댑터로 경계를 대신하는 검사와 실제 Oracle 검증을 구분합니다.

검토한 모든 알고리즘을 이번에 설치한 것은 아닙니다. Desbordante, 공식 Banff, EBM, PyOD 등은 향후 Render 설치·실행·자원·품질 비교 후보입니다. 기존 선택적 PySR 경로를 새 통합 실행의 필수 의존성으로 추가하지 않았습니다. 독립 최종 평가, 여러 규칙의 셀별 근거 통합·우선순위·오류 위치 추정, 완전한 시나리오 데이터/설정 스냅샷도 별도 검증·개발 범위입니다.

GS 인증·납품 검증을 준비할 때에는 실제 배포 버전, 지원 Oracle/Render 환경, 설치 상태, 옵션, 시험 데이터·결과 및 알려진 제한을 함께 기록합니다. 상세 혼합형 알고리즘·화면·설치 이력은 [MIXED_XAI_GUIDE.md](MIXED_XAI_GUIDE.md), 재발 방지 항목은 [QUICK_EDIT_RELIABILITY.md](QUICK_EDIT_RELIABILITY.md)를 참조합니다.

### 2026-09-25 소스 검증 기록

- 작업 위치: `D:\work\data-editing-system`, 브랜치: `codex/integrated-editing`.
- `scripts/test_offline.py`: 백엔드 400개 통과. `.env`, Oracle 연결 및 네트워크 연결을 차단했습니다.
- `node --test quick-edit/tests/*.test.js`: 브라우저 상호작용을 포함한 프런트 검사 106개 통과.
- 보고서 공존·구형 스키마·긴 수식 및 기존 구조화/사용자 보고서 관련 검사 49개 통과. 일부는 위 백엔드 검사와 중복됩니다.
- Python 컴파일, 변경 JavaScript 구문 검사, 프리셋·FLOW JSON 파싱, `git diff --check` 통과.
- 실제 Oracle 프로시저/실행계획, Render 빌드·운영 실행, 고객 정답 데이터에 대한 최종 탐지 품질 비교는 검증하지 않았습니다. DB DDL 적용·Git 푸시·운영 배포는 수행하지 않았습니다.

후속 화면 보강에서는 퀵 신규 실행의 통합형 고정, 범주형·연속형·혼합형 결과의 단일 탭 선택과 정확한 조회 연결, 한국어·영어 안내를 검증했습니다. 프런트 전체 115개와 퀵 이력·제어·컬럼 유형 관련 백엔드 55개가 통과했으며, 실제 스타일을 적용한 탭 클릭·선택 복원·지연 응답과 과거 작업 재개를 포함합니다. 기존 실행의 데이터·수식·DB 식별자는 변경하지 않습니다.
