# 혼합형 XAI 실제 값·범위·수식 규칙 시나리오 (4단계)

기존 4단계 FLOW와 범주형/연속형 구분을 유지합니다. 새 **통합 에디팅(`UNIFIED`)**은 각 단계의 기존 Oracle 분석에 이 문서의 혼합형 기초통계·관계·규칙·위반 분석을 보강합니다. 별도로 저장된 `MIXED_XAI` 4단계 FLOW/JOB도 계속 사용할 수 있으며 기존 작업을 자동 변환하지 않습니다. 통합 흐름의 운영·배포·제한은 [INTEGRATED_EDITING_GUIDE.md](INTEGRATED_EDITING_GUIDE.md)를 참고합니다.

혼합형 보강은 유형 결과를 대체하거나 원본 자료형을 변경하지 않습니다. 표본 값의 숫자 해석 가능성과 실제 컬럼 관계를 별도로 검사하므로 선행 범주 판정만으로 혼합형·수식 탐색 전체를 차단하지 않습니다.

## 변경한 학습 목표

v1의 `IF 조건 THEN ANOMALY_CANDIDATE`는 Isolation Forest 판정을 설명했습니다. 현재는 실제 컬럼 Y를 나머지 컬럼으로 예측합니다. Y에서 파생된 원핫·결측 표시까지 IF 입력에서 제외하며, 실제 값·명시적인 수치 구간·허용오차가 있는 수식을 발굴합니다.

예를 들어 `IF 직업코드 = 'A' THEN 고용형태코드 = '1'`이면, 직업코드는 A인데 고용형태코드가 1이 아니거나 NULL인 행을 찾습니다. 이는 출력 형식 예시이며 사용자 데이터에서 발견한 규칙이 아닙니다. 연속 수치는 `매출액 > 100 AND 매출액 <= 500`처럼 실제 경계를 표시합니다.

1. **MIXED_XAI_PROFILE (M03001):** 물리 자료형과 표본 값을 확인해 결측·고유값·상위 빈도·최소/최대·평균·표준편차·사분위수·중복 행을 분석합니다. 업로드로 문자형에 저장된 숫자도 엄격한 변환 기준을 충족하면 수치 통계를 계산합니다. 원본 자료형 변경이나 수동 범주형/연속형 확정은 요구하지 않습니다.
2. **MIXED_XAI_RELATION (M03002):** 수치 컬럼의 Pearson 상관, 범주형 쌍의 편향 보정 Cramér's V, 범주형–연속형의 상관비 η와 유효 짝 수·자료 존재율, 동일 컬럼, 결측 현황을 분석합니다. 관계 진단은 규칙이나 인과관계 판정이 아닙니다.
3. **MIXED_XAI_RULE_DISCOVER:** 자동 전처리 → 실제 컬럼별 값/구간 트리 및 연속형 수식 후보 → 분리된 검증 표본으로 규칙 선별.
4. **MIXED_XAI_RULE_DETECT:** 전체 원본에서 A와 B의 빈도를 집계하고 **A는 만족하지만 B는 성립하지 않는 행**을 조회·저장.

Oracle의 `NOT B`만 사용하면 NULL이 누락됩니다. 탐지·실시간 조회·최종 에디팅 재검증 모두 `A AND CASE WHEN B THEN 0 ELSE 1 END = 1`을 적용합니다. 혼합형 보강 자체는 기존 scikit-learn, NumPy, SciPy를 재사용하며 신규 패키지, OML4Py 또는 물리적 Oracle ML 모델 생성이 필요 없습니다. 통합 4단계의 기존 Oracle 분석은 종전처럼 설치된 OML 기능과 모델 실행 경로를 사용합니다.

## 수동 SQL과 모델 등록

DB 오브젝트/DDL은 자동 실행하지 않습니다.

- **XAI v1이 이미 설치된 Target DB:** `database/INIT_TARGET_PATTERN.sql`을 수동 실행합니다. 기존 `INIT$_TB_RULEDISC_ASSOC_SUM`에 nullable 컬럼 5개를 추가합니다.
- **XAI를 처음 추가하는 기존 Target DB:** `database/INIT_TARGET_XAI.sql`을 실행합니다. 진단·과거 호환용 XAI 테이블과 같은 패턴 확장을 포함합니다.
- **신규 전체 설치:** `database/INIT_TARGET_DDL.sql`에 포함했습니다. 전체 업그레이드용 `INIT_TARGET_ALTER.sql`에도 동일한 확장이 있습니다.
- 설치 목적으로 DROP/TRUC 파일을 실행하지 않습니다. System DB 변경은 없습니다.

SQL 적용 후 백엔드를 재시작해 `MIXED_PATTERN.sql` 등의 SQL ID를 다시 로드하고 브라우저를 새로고침합니다.

`409: Run database/INIT_TARGET_PATTERN.sql ...`로 멈추면 새 규칙 컬럼이 설치되지 않았는지 확인합니다. 이전 설치 블록에는 지역 프로시저 뒤에 변수가 선언된 PL/SQL 순서 오류가 있었습니다. 수정된 `INIT_TARGET_PATTERN.sql` 전체를 앱과 같은 Target DB 계정으로 다시 실행하세요. 첫 조회의 DB/계정을 확인하고, 출력에 `[OK] Mixed pattern schema: all 5 rule columns installed`가 나온 뒤 **실패 단계부터 다시 시도**하면 됩니다. 이미 있는 컬럼과 결과 데이터는 유지됩니다. 수정된 백엔드는 누락된 컬럼 이름도 오류에 표시합니다. 실제 DB 설치 상태는 오프라인 검증만으로 확인할 수 없습니다.

| 메뉴 | 등록 내용 |
|---|---|
| M90001 내부모델 | 추가 등록 없음. 기존 모델 유지 |
| M90002 외부모델 | 중앙에서 기본 설정을 관리할 때 **Mixed XAI Python API** 그룹의 4개 API를 등록합니다. 퀵 에디팅·시나리오 기본 설계는 미등록 내장 API를 앱 프리셋으로 준비하므로 추가 등록이 필수는 아닙니다. |

내장 API는 `MIXED_XAI_PROFILE`, `MIXED_XAI_RELATION`, `MIXED_XAI_RULE_DISCOVER`, `MIXED_XAI_RULE_DETECT`입니다. 활성 등록이 있으면 그 설정을 우선 사용합니다. 미등록이면 서버가 배포된 프리셋의 입력·출력 계약을 검증한 뒤 해당 시나리오의 JOB에 저장합니다. 전역 모델 등록과 DB 오브젝트 생성은 하지 않습니다. 비활성 또는 잘못된 등록은 임의로 대체하지 않으며 M90002에서 수정해야 합니다.

신규 통합 FLOW는 위 혼합형 단독 API 대신 `UNIFIED_EDITING_PROFILE/RELATION/DISCOVER/DETECT`를 사용합니다. M03001~M03004에서 자동 생성 JOB을 열 때에는 중앙 등록 ID가 없는 내장 JOB도 저장된 계약으로 모델과 파라미터를 복원합니다. M90002 기본 생성은 설정 파일의 기본 대상 중 미등록 항목만 추가하며, 이미 등록된 항목과 저장 JOB은 보존합니다. 혼합형 단독 API의 중앙 등록이 필요하면 해당 프리셋을 선택해 검토·저장합니다.

`Default Python API resource MIXED_XAI_PROFILE is not registered or active.`로 5단계에서 멈춘 경우, 이 수정의 백엔드·프리셋·`database/MCOM_DATA_WORK.sql`을 함께 배포하고 서버를 재시작한 뒤 **실패 단계부터 다시 시도**합니다. 이 SQL 파일은 서버가 읽는 조회문 모음이며 수동 DDL 실행 파일이 아닙니다. 이번 오류 수정에 추가 DDL은 없습니다. 기존 업로드와 대상 테이블을 재사용합니다. 작업 상세 화면도 새 프론트 파일이 적용되도록 새로고침합니다.

기존 API 이름과 엔드포인트는 유지합니다. 프리셋에서 등록을 갱신하면 새 기본 파라미터와 결과 테이블을 반영할 수 있습니다. 기존 등록의 `P_CONTAMINATION`은 v2에서 사용하지 않습니다. 기존 JOB의 저장된 파라미터는 유지되므로 필요한 기준은 해당 모델/JOB 파라미터에서 갱신합니다.

새 템플릿의 1·2단계 결과는 `INIT$_TB_XAI_RUN.SUMMARY_JSON`의 `profile`·`relationships`에 함께 저장합니다. 새 DB 오브젝트는 추가하지 않습니다. 발굴 `INIT$_TB_RULEDISC_ASSOC_SUM`, 탐지 `INIT$_TB_RULEVIOL_ASSOC`입니다. 이전 JOB에 `_XAI` 결과 이름이 남아 있어도 혼합형 실행 API와 결과 버전을 통해 실제 저장 결과로 연결합니다.

## 연속형 수식 규칙과 기본 설정

파일 입수는 원본 값을 보존하기 위해 데이터 컬럼을 `VARCHAR2`로 저장합니다. 연속형 발굴은 물리 숫자형뿐 아니라 **학습 표본에서 숫자로 확인된 문자형 컬럼**도 분석합니다. 부호·소수점·지수 표기만 허용하고, `001` 같은 선행 0 코드와 쉼표·통화·문자 혼합값은 임의 변환하지 않습니다. 숫자 해석은 학습·Oracle 탐지·실시간 조회·최종 에디팅에서 같은 기준을 사용합니다. 입력 컬럼이 변환 불가이면 수식 적용 대상에서 제외하고, 입력은 유효한데 결과가 결측 또는 변환 불가이면 위반으로 처리합니다.

수식 발굴은 fit 구간의 비어 있지 않은 값 중 98% 이상이 엄격하게 숫자로 해석되고 최소 유효 건수(기본 30건)를 만족하면 일부 오타가 있어도 컬럼을 분석합니다. 오타 원본은 남겨 두고 fit·보정·검증 구간별 변환 불가 건수를 기록합니다. 선행 0 코드가 관측되면 계속 숫자 수식에서 보호하며, 결측·변환 불가 값을 0으로 학습하지 않습니다.

퀵 에디팅과 상세 분석은 값·구간 규칙과 연속형 수식 규칙을 구분해 표시합니다. 수식이 0건이어도 탭을 숨기지 않고 실행 여부, 수치 컬럼 수, 제외·검증 사유를 보여줍니다. 이전 실행에 수식이나 진단이 저장되지 않았다면 새 FLOW 실행이 필요합니다. 화면 새로고침만으로 과거 결과에 규칙을 추가하지 않습니다.

수식 규칙은 `IF 적용 조건 THEN 결과 컬럼 ≈ f(다른 컬럼) ± 허용 오차`로 표현합니다. 행마다 계산한 기대값과 실제값·오차를 비교하여 위반을 찾습니다. 단순 구간으로만 설명하기 어려운 연속형 관계를 보완하며, 결과 컬럼은 수식 입력에서 제외합니다.

합·차 관계는 `A ≈ B + C`, `A ≈ B - C`처럼 실제 구성항을 직접 검사합니다. 개별 상관이 낮은 작은 구성항도 합쳐 보면 관계가 성립할 수 있으므로, 단변수 상관 순위 앞부분만 회귀하는 방식에 의존하지 않습니다. 일반 계수의 선형식은 현재 후보가 설명하지 못한 잔차를 이용해 추가 입력을 선정합니다. 탐색은 표본·후보 수를 제한하며 가능한 모든 수식을 전수 검색하는 것은 아닙니다.

합·차 후보 선별은 학습 자료 최대 256행을 사용하고, 3~4항으로 확장할 때 최대 12개 후보만 유지하며, 각 대상·조건별 최대 8개 후보를 본검사합니다. 기본 최대 입력 수는 기존 `P_MAX_FORMULA_FEATURES=3`이며 4까지 설정할 수 있습니다. 입력 수 1로 설정하면 두 컬럼을 쓰는 합·차와 비율식은 탐색하지 않습니다. 선별된 식은 전체 fit·보정·검증 구간으로 평가합니다. 별도 모델 설치나 학습 라이브러리 추가 없이 기존 NumPy 경로를 재사용합니다.

종류가 적은 정수형 건수도 학습에서 네 종류 이상의 비음수 값과 강한 합계 근거가 있으면 단위 합계 후보로 검사합니다. 이 예외는 일반 회귀로 범주 코드를 예측하도록 확대하지 않습니다. 학습 IQR이 0인 컬럼처럼 변화 근거가 부족한 경우는 계속 제외하고 진단 사유를 남깁니다.

정수·간단한 분수로 계수를 단순화할 때는 학습 예측값과 오차를 확인하고 단순화한 AST 자체를 다시 검증합니다. 따라서 `11.999999… × X`를 단순히 화면에서만 `12 × X`로 반올림하지 않습니다. 저장된 수식으로 기대값·허용범위·위반 여부·최종 수정 검증을 계산합니다. 퀵 에디팅과 상세 분석에는 결과 수식 전체와 합·차/선형 회귀/비율 관계의 구분을 표시합니다. 기존 실행은 당시 저장된 수식과 지표를 유지하며 개선된 발굴은 새 실행에서 적용됩니다.

허용 오차의 계산 하한은 수식 항의 규모와 부동소수점 정밀도에서 구합니다. 큰 금액에 비해 작은 구성항을 고정 상대오차로 무시하지 않으면서, Python 학습과 Oracle 숫자 계산 사이의 반올림 차이도 고려합니다. 보정 잔차가 더 크면 그 분포에서 정한 오차를 사용하고, 기존 최대 IQR 비율과 fit·검증 기준을 모두 통과해야 합니다.

학습 자료는 **계수 학습(fit)·오차 보정(calibration)·검증(validation)**에 겹치지 않게 분리합니다. 보정 표본의 잔차 분포로 허용 오차를 정하고, 검증 표본에서 허용 오차 안에 들어오는 비율과 설명력을 확인합니다. 이 허용 오차는 통계적인 95% 예측구간을 뜻하지 않습니다. 규칙 선택에 검증 표본을 사용하므로 독립 최종 시험 성능으로 해석하지 않습니다.

전체 수식이 채택되어도 2~4개 범주의 제한된 조건부 집단을 추가 검사합니다. 같은 학습 집단의 중복 후보와 전체 수식으로 이미 설명되는 후보를 줄입니다. 대안 집단 수식은 기존 품질 기준과 학습·선택 검증의 최소 5%p 포함 비율 개선을 통과해야 합니다. 채택된 대안 집단은 전체 수식의 IF 적용 범위에서 제외하고 나머지 지표를 재검증하므로 정상 소수 집단을 전체 수식의 위반으로 중복 지적하지 않습니다. 이때 기존 계수·허용오차는 다시 학습하지 않습니다.

- `P_CONTINUOUS_ENABLED=true`: 연속형 수식 발굴을 사용합니다.
- `P_MAX_TARGETS=64`: 값/구간 트리의 최대 결과 컬럼 수입니다. 수식 분석은 별도 한도 `P_MAX_CONTINUOUS_TARGETS=16`을 적용합니다.
- `P_MAX_CONTINUOUS_RULES=32`, `P_MAX_FORMULA_FEATURES=3`: 수식 후보를 제한합니다. 최종 결과에는 전체 `P_MAX_RULES=64` 한도도 적용합니다.
- `P_MIN_FORMULA_CONFIDENCE=0.95`, `P_MIN_FORMULA_VALIDATION_CONFIDENCE=0.90`: fit·검증 표본에서의 허용 오차 이내 비율 기준입니다.
- `P_MIN_FORMULA_R2=0.90`: 허용 오차 이내 행의 fit·검증 R² 기준입니다. 전체 행의 R²·MAE와 포함 비율을 함께 보존하며, 전체 행 성능과 구분해서 표시합니다.
- `P_MAX_TOLERANCE_FRACTION=0.05`: 허용 오차가 대상 컬럼 학습 IQR의 5%를 넘는 후보는 제외합니다. 허용 설정 범위는 0.001~0.2입니다.

기존에 저장된 JOB 파라미터는 자동으로 덮어쓰지 않습니다. 프리셋 갱신 후 필요한 값은 해당 JOB에서 갱신합니다. 혼합형 4단계 템플릿을 만들거나 새 퀵 에디팅을 실행하면 기초통계·관계 진단 단계가 포함됩니다.

## 결과 재사용과 지표

| 오브젝트 | v2 저장 내용 |
|---|---|
| `INIT$_TB_RULEDISC_ASSOC_SUM` | 기존 공통 규칙, 실제 THEN 컬럼·값·텍스트, 지지도·신뢰도·향상도·분모 |
| 위 테이블 추가 컬럼 | `CONDITION_JSON`, `RESULT_JSON`, `VALIDATION_JSON`, `RESULT_KIND`, `VIOLATION_COUNT` |
| `INIT$_TB_RULEVIOL_ASSOC` | 공통 위반 행, 기대값·실제값·원본 위치·위반 사유 |
| `INIT$_TB_XAI_RUN` | 기초통계·관계 진단(`profile`, `relationships`), 버전, 표본·제외 컬럼·설정·검증 진단·전체 집계 |

`RESULT_KIND`는 `VALUE`, `RANGE`, `FORMULA`로 결과 의미를 구분합니다. `RULE_SOURCE`/`MODEL_TYPE`은 `MIXED_PATTERN_TREE`이며 `XAI_PATTERN_<실행ID>`는 논리적인 이름입니다. Oracle ML 오브젝트로 생성하지 않습니다. 모든 읽기·쓰기·정리는 실행 종류/ID/대상 소유자/테이블 및 모델 범위를 유지합니다.

신뢰도는 `n(A AND B)/n(A)`, 지지도는 `n(A AND B)/N`, 향상도는 값/구간 규칙의 `P(B|A)/P(B)`입니다. 수식 규칙의 신뢰도는 조건 해당 행 중 허용 오차 이내 비율이며, 수식에 맞지 않는 향상도는 빈 값으로 둡니다. 발굴 직후에는 학습 표본 기준이고 탐지 후에는 **동일한 전체 적용 데이터 기준**으로 공통 지표를 다시 계산합니다. 학습 수치와 검증 수치는 별도로 보존합니다. 검증 표본은 규칙 선택에도 사용하므로 독립 최종 시험 정확도로 해석하지 않습니다.

규칙×행 위반 건수, 고유 위반 행 수, 저장된 미리보기 수를 구분합니다. 통계적 패턴도 업무상 예외를 포함할 수 있으므로 위반을 자동 수정·승인하지 않습니다. 개별 행의 오류 확률에 해당하는 점수는 없으므로 임의로 만들지 않습니다.

## 공통 화면·최종 에디팅

- 파일 찾기 아래 새 작업의 기본값은 **통합 에디팅 4단계**입니다. 기존 분석·혼합형 단독 선택도 유지하고 저장된 작업은 당시 선택을 복원합니다.
- FLOW 기본 템플릿에서 통합 4단계, 기존 4단계, 혼합형 4단계를 설계합니다. 통합 템플릿은 `:UNIFIED:V1`, 혼합형 템플릿은 `:MIXED_XAI:V3` 표식으로 구분합니다. 통합 템플릿은 각 단계 안에서 기존 분석과 혼합형 보강을 실행합니다. 과거 2단계 실행·저장 FLOW와 사용자가 수정한 FLOW는 덮어쓰지 않습니다.
- 통합 실행 결과에서는 기존 OML 결과와 혼합형 보강 결과를 선택해 확인합니다. 양쪽 위반은 겹칠 수 있으므로 합산 건수를 고유 오류 행·셀 수로 보지 않습니다.
- 퀵 에디팅과 M04002 모두 공통 IF–THEN 요약, 조건 수·결과 컬럼 필터, 기대값·실제값·신뢰도·향상도, 위반 표·페이지 이동을 사용합니다. 혼합형에는 검증 표본 건수·신뢰도 및 표본 제한 안내가 추가됩니다.
- NULL 실제값은 `NULL(결측)`로 표시해 실제 문자 `-`와 구분합니다. 신규 표시와 위반 사유는 한국어·영어를 제공합니다.
- M04002는 저장 미리보기와 현재 원본을 다시 평가하는 실시간 조회를 같은 팝업에서 제공합니다. 원본 상세는 CASE_ROWID로 연결합니다.
- 최종 에디팅 검토·수정 후 재검증도 같은 IF/THEN AST를 사용합니다. 값 규칙은 기대값을 사용할 수 있고 범위 규칙은 구간 문구를 데이터에 쓰지 못하게 검증합니다. 범위 안의 구체적인 수정값은 사용자가 지정합니다.
- 수식 규칙도 M05001의 연속형 목록에서 검토합니다. 수정값은 현재 수정 테이블의 입력 컬럼으로 계산한 행별 허용범위와 서버에서 다시 비교합니다. 화면이 보낸 예상값을 신뢰하지 않으며, 수식 문자열을 데이터에 저장하지 않습니다. 최종 효과 분석의 연속형 오차 비교와 전체 위반 재검증을 재사용합니다.
- 혼합형도 공통 기초통계를 계산합니다. 컬럼별 위반은 실제 THEN 컬럼에 집계합니다. 수정 테이블이 있으면 기존 전후 비교를 재사용합니다.

과거 v1의 이상 후보 이력은 보존합니다. 실제 THEN이 없으므로 v2 값 규칙으로 변환할 수 없습니다. **실제 기대값과 위반 데이터를 얻으려면 새로 실행해야 합니다.**

## 기초통계·관계 분석의 범위

1·2단계와 혼합형 발굴은 설정된 행/컬럼 및 약 32 MiB 보관 표본 제한 안에서 분석합니다. 새 통합 프리셋은 `HASH`, 전략이 없는 기존 JOB은 `FIRST_ROWS`를 사용합니다. `HASH`는 전체 건수 조회와 ROWID 해시 후보 스캔 뒤 해시 우선순위 표본을 유지하므로 앞부분 조회 편향을 줄이지만 전체 스캔 비용이 발생할 수 있습니다. 층화·스냅샷 고정은 제공하지 않으며 같은 seed·ROWID·값이 재현성의 전제입니다. 바이트 제한으로 줄어든 표본의 대표성을 보장하지 않습니다. 표본 수·한도·비용·지원하지 않는 컬럼·선택에서 빠진 컬럼을 기록합니다. 중복 행은 **표본의 선택 컬럼 조합** 기준입니다.

관계 단계는 최대 64개 수치 컬럼의 유효 짝을 계산하고 상관 절대값이 큰 최대 100개 짝을 보여 줍니다. 결측이 있는 짝은 두 값이 모두 존재하는 행만 사용하며 유효 짝 수와 자료 존재율을 함께 표시합니다. 상수 컬럼 또는 유효 짝이 부족한 경우 상관값은 없습니다. 원본 메타데이터의 컬럼 설명도 저장합니다. 비유한 수치 입력은 통계에서 제외한 건수와 경고로 표시하며, 계산 범위를 벗어난 통계량은 빈 값과 `STATISTIC_OUT_OF_RANGE` 경고를 남깁니다.

범주형 관계는 최대 32개 컬럼·컬럼당 32개 범주를 사용하며 범주형/혼합형 각각 최대 256개 후보 쌍을 평가해 상위 100개를 보관합니다. 희소 기대빈도와 최소 집단 건수, 한도로 제외된 후보를 함께 기록합니다. 진단에서 관계가 약하거나 표시 목록에 없다는 이유만으로 후속 수식 탐색을 모두 금지하지 않습니다.

## 실행 제어·그래프

퀵 에디팅의 일시 멈춤/중단은 현재 요청 또는 FLOW 노드가 끝난 뒤 적용됩니다. 재개는 같은 실행 ID에서 미완료 노드부터, 중단 후 재시작은 새 실행 ID로 FLOW를 시작합니다. 진행 중인 Oracle SQL/Python 학습을 강제 종료하거나 완료 작업을 롤백하지 않습니다.

기존 연속형 차트는 회귀선, 95% 평균 응답 신뢰구간(CI), 95% 개별 응답 예측구간(PI)을 구분하고 PI 밖 점을 삼각형으로 표시합니다. 표시 표본의 선형 진단이며 규칙 위반 판정과 별개입니다.

기존 M04002 그래프도 휠 확대의 실제 축 범위를 50~800%로 제한하고, 범례를 키보드로 선택할 수 있습니다. 숨긴 표본은 그래프 방향키 탐색에서 제외하며 표의 행을 직접 선택하면 해당 점을 표시합니다. 기존 퀵 에디팅은 좁은 화면과 컨테이너 크기 변경에 맞춰 다시 그리며, 그래프 방향키 탐색 중 포커스를 유지합니다. 최대화·종료·화면 복귀와 좌표 계산도 기존·혼합형에서 함께 회귀 검사합니다.

혼합형 수식은 M04002의 연속형 규칙 카드·상세 표·위반 결과에서 **수식 그래프 보기**로 확인합니다. 퀵 에디팅의 연속형 규칙 상세에도 같은 그래프를 제공합니다. 기존 그래프와 동일하게 가로축은 실제값, 세로축은 저장된 수식의 예측값이며 정상은 파란 원, 위반은 빨간 삼각형입니다. 기준선과 저장된 허용오차의 상·하한을 구분하고, 잔차 보기로 전환하거나 점·표의 행을 선택하면 해당 행의 기대값·실제값·허용범위와 수식 입력값을 볼 수 있습니다. 잔차 그래프는 가로축 예측값·세로축 실제값−예측값입니다. 새 회귀선을 학습하지 않으며 이 경계는 95% CI/PI가 아닙니다.

그래프 바로 위에 기존 상세 그래프와 같은 공통 툴바를 사용합니다. 확대·축소(50~800%), 마우스 휠 켜기/끄기, 보기 초기화, 최대화/원상복구, 확대율 순서입니다. 마우스 휠은 포인터를 중심으로 확대하며 드래그로 이동할 수 있습니다. 아래 범례를 누르면 정상·위반·허용경계·기준선을 각각 표시하거나 숨깁니다. 선택점과 선택 행은 기존 화면과 같은 황색으로 강조하며 모드를 바꾸면 확대율은 100%로 돌아갑니다. 최대화 중 Escape는 그래프를 먼저 복원합니다. 툴바 스타일은 `frontend/css/rule-chart-controls.css`에서 공통 관리합니다.

수식 배너·복사 버튼도 위 공통 CSS를 사용합니다. 점의 채움·테두리·크기·선택 색상 및 기준선·경계선은 `RegressionDiagnostics.chartStyle`을 기존 Chart.js, 퀵 에디팅 canvas, 혼합형 SVG가 함께 사용합니다. 선택된 점은 원래 원/삼각형 모양을 유지하면서 황색으로 채워집니다. 허용오차가 매우 작아 기준선과 겹치는 경우에는 경계를 임의로 넓히지 않고 잔차 그래프를 안내합니다.

혼합형도 그래프 바로 아래에 표시 표본 전체(최대 300행)의 스크롤 그리드를 제공합니다. No·원본 행 식별자·X 입력 컬럼·Y 실제 결과값·기대값·잔차·허용범위·판정을 함께 표시하고, 컬럼 설명 및 X/Y 배지를 제공합니다. 점 선택 시 그리드 내부를 해당 행으로 이동하고, 행 클릭/방향키 선택 시 해당 점을 표시합니다. 확대·잔차 전환·최대화 후에도 선택과 그리드 스크롤을 유지합니다. No와 헤더는 스크롤 중 고정됩니다. 과거 응답에 입력 컬럼 메타가 없어도 표본 값에서 컬럼을 구성하고 저장된 요약의 설명을 재사용합니다.

M04002의 값·구간/연속형 결과 탭은 기존 통합 결과의 pill 탭 스타일을 재사용합니다. 혼합형 수식 카드의 수식·복사·그래프·위반 조회는 기존 연속형 카드 구성을 사용하고, 연속형 위반 요약·필터·카드는 기존 주황색 스타일을 공유합니다. 혼합형에 적용하지 않는 기존 모델의 max-rules·lift 탐지기준은 표시하지 않습니다.

그래프는 **조회 시점의 현재 원본** 앞부분 최대 5,000행을 평가하고, 조건을 만족하는 정상·위반에서 나누어 뽑은 최대 300점을 표시합니다. 과거 실행 당시의 스냅샷이나 전체 원본 통계가 아닙니다. 표시 점의 위반 비율을 전체 위반율로 해석하지 않도록 조회 시각·표본 한도·표시 건수를 함께 안내합니다. 결과값이 결측·숫자 변환 불가이면 위반으로 집계하되 0으로 그리지 않고 그래프 제외 건수를 표시합니다. 위반 여부와 잔차는 서버의 저장 규칙 평가 결과를 사용합니다.

저장된 `FORMULA` 규칙이 있다면 과거 실행도 재학습 없이 그래프를 열 수 있습니다. 그래프 추가에는 새 모델 등록·DB 오브젝트·수동 DDL이 필요하지 않습니다. `backend/services/mixed_formula_sample_service.py`, 런타임 조회문 `database/MIXED_PATTERN.sql`, 공통 `formula-rule-chart.js`/`.css`와 양쪽 화면 파일을 함께 반영한 후 서버 재시작·브라우저 새로고침이 필요합니다.

## 기본 제한과 해석

- 학습: 지정된 표본 전략으로 최대 25,000행, 보관 표본 약 32 MiB, 최대 128개 입력 컬럼/4,096개 인코딩 특징. 서버·사용자 자원 설정에 따라 실제 한도는 더 작을 수 있습니다. 보관 표본의 추정 크기는 프로세스 전체 메모리 상한이 아니며, 표본 방식·제한 도달 여부·제외 컬럼을 기록합니다.
- 실제 결과 컬럼: 최대 64개 자동 선택. 더 많은 컬럼은 `targetColumns`/`P_TARGET_COLUMNS`로 관심 대상을 지정해 별도 실행합니다. 숫자라도 고유값 수가 적으면 실제 값으로, 연속 수치는 학습 표본의 분위 구간으로 처리합니다.
- 기본 규칙: 최대 64개, 트리 깊이 4, 학습 신뢰도 0.99, 검증 신뢰도 0.90, 검증 조건 해당 최소 10행, 향상도 1.01 및 무조건부 빈도 대비 개선 요구. 모델 옵션으로 조정할 수 있습니다. 검증 건수 10행은 강한 보증이 아니며 실제 건수를 함께 판단해야 합니다.
- 식별자·상수·숫자로 해석할 수 없는 고카디널리티 문자열과 목표 컬럼 자체로 만든 조건은 배제합니다. 숫자 문자열은 별도로 연속형 적합성을 판단합니다. 근거가 부족하면 **규칙 0개**를 정상 반환하며 기준을 임의로 낮추지 않습니다.
- 저장 미리보기는 전체 규칙이 최대 1,000행을 공유하되 규칙별로 배분합니다. 전체 위반 건수는 저장 상한과 관계없이 집계합니다. 원본 전체를 복제하지 않습니다.
- SQL 값은 바인드하고 식별자는 물리 메타데이터로 검증합니다. IF의 float32 분기와 THEN의 원래 수치 경계를 구분합니다. 원본 재생성·이동 후에는 과거 ROWID가 유효하지 않을 수 있습니다.

## 검증 범위

모델 미등록·일부 등록·비활성·사용자 설정 유지, JOB 저장 후 실행, 재시도 중복 방지를 실제 준비 함수와 배포 프리셋으로 검사합니다. 반복 장애의 원인과 변경 시 필수 점검은 [QUICK_EDIT_RELIABILITY.md](QUICK_EDIT_RELIABILITY.md)에 기록합니다.

오프라인 합성 데이터로 실제 값·범위 규칙, 목표값 누출 방지, NULL 위반, 신뢰도·분모, 별도 검증, 무관 데이터의 규칙 억제, SQL 스키마·바인드·범위, 기존 이력, 퀵/상세 화면 및 최종 에디팅을 회귀 검사합니다. 실제 Oracle 설치 및 사용자 데이터의 처리시간·규칙 품질은 수동 SQL 적용 후 확인해야 합니다.
