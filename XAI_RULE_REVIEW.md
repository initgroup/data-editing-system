# 혼합형 XAI 결과와 실제 에디팅 규칙 목적 비교

검토일: 2026-09-23. 사용자 캡처 2개, 최초 첨부 `내용.txt`, 현재 소스와 SQL을 비교했다. 실제 운영 DB에는 접속하지 않았다. 이 문서는 기존 v1의 문제 분석과 개선 설계 기록이다. 이후 실제 컬럼별 예측 트리 기반 v2를 구현했다. 현재 설치·동작 기준은 MIXED_XAI_GUIDE.md를 따른다. 과거 이상 후보 이력은 보존하며 실제 THEN 결과를 얻으려면 v2로 새로 실행해야 한다.

## 1. 결론

현재 구현의 핵심 학습 대상은 실제 결과 컬럼이 아니라 Isolation Forest의 이상 판정이다. 따라서 `IF 조건 THEN 이상 후보`는 생성하지만, 사용자가 원하는 `IF 조건 THEN 실제 컬럼의 값/구간`과 그 규칙의 위반을 생성하지 않는다.

앞선 화면 공통화는 이 목적 차이를 해결하지 못했다. 현재 XAI 순도를 신뢰도로 바꾸거나 가상 결과 컬럼에 실제 컬럼명을 붙이는 방식으로 고칠 수 없다. 실제 결과 컬럼을 학습하는 규칙 엔진으로 변경하고 새로 실행해야 한다.

최초 첨부 자료에는 Isolation Forest 대리모델 설명과 연관규칙을 중심으로 한 하이브리드 접근이 함께 있었다. 대리모델 방식은 이상 판정을 설명하는 데 맞지만, 사용자가 명시한 최종 목표인 실제 값의 패턴 규칙 발굴을 대신하지 못한다. 사용자 목표를 우선하여 설계를 수정해야 한다.

## 2. 화면에서 관찰된 현상의 원인

| 항목 | 기존 실제 값 규칙 | 현재 XAI |
|---|---|---|
| THEN | `COL012 = 2`처럼 실제 컬럼의 기대값 | `ANOMALY_CANDIDATE`라는 가상 이상 분류 |
| 신뢰도 | 조건에 맞는 행 중 실제 THEN이 성립하는 비율 | 계산하지 않음. 별도 모델 일치율은 다른 의미 |
| 위반 탐지 | 조건은 맞지만 실제 결과가 기대값과 다른 행 | IF 조건에 맞는 모든 행 |
| 기대값·실제값 | 기대값과 원본 결과 컬럼 값 | 조회 어댑터가 NULL을 반환 |
| 원본 상세 | 실제 결과 컬럼을 포함할 수 있음 | 저장 미리보기는 IF에 사용된 컬럼만 포함 |
| 건수 | 전체 위반과 저장 미리보기 구분 | 전체 조건 해당 건수와 저장 미리보기 구분 |

구체적인 근거:

- `backend/services/mixed_xai_algorithm.py`, `analyze_mixed_rows`: `labels = scores > threshold`, `tree.fit(..., labels[train_indices])`로 이상 판정을 학습하며 `THEN ANOMALY_CANDIDATE`를 고정 생성한다.
- `backend/services/mixed_xai_service.py`, `_rule_summary`: `RESULT_COLUMN = ANOMALY_CANDIDATE`, `RESULT_VALUE = Y`, `RULE_CONFIDENCE = None`, `RULE_LIFT = None`을 반환한다. 실제 결과 컬럼이 없어도 현재 매핑 건수는 성공한 것으로 집계한다.
- 같은 파일의 `detect`: 조건 AST만 컴파일해서 개수와 행을 조회한다. 결과 조건 B의 불성립을 확인하는 로직이 없다.
- `frontend/js/rule-result-common.js`, `candidateRows`: 기대값·실제값·신뢰도·향상도를 NULL로 채운다.
- `database/MIXED_XAI.sql`, `XAI_MATCH_PREVIEW`: `WHERE /*PREDICATE*/`로 조건 일치 행을 읽는다.

캡처의 모델 일치율 **75.8%**는 현재 규칙의 학습 조건 해당 33건 중 약 25건이 Isolation Forest의 이상 판정과 일치한다는 뜻이다. 실제 결과 컬럼의 기대값을 75.8% 맞힌다는 뜻이 아니다. 검증 75%도 조건 해당 4건 중 3건 수준이며, 적은 표본으로 확정적인 업무 규칙이라고 볼 수 없다.

`검토 대상 645`와 `미리보기 289`는 같은 모집단의 수가 아니다. 현재 저장 상한은 기본 최대 1,000건을 전체 규칙이 공유하고 앞 규칙부터 소진한다. 따라서 특정 규칙의 전체 조건 일치 건수보다 저장 행이 적거나 0일 수 있다. 캡처 실행의 실제 저장 내역은 조회하지 않았으므로 289건의 정확한 배분 이력은 미확인이다.

기존 화면의 실제값 `-`는 별개로 점검해야 한다. 기존 저장 SQL은 원본 결과 컬럼을 기록하고 NULL을 위반에 포함한다. 프론트가 NULL·빈 문자열과 실제 문자 `-`를 동일하게 보이게 하므로 캡처만으로 값 누락이라고 단정할 수 없다. `NULL(결측)`, 실제 문자 `-`, `해당 없음`을 구분하도록 개선하는 것이 타당하다.

## 3. 대안 비교와 권고

| 대안 | 목적 적합성 | 현재 프로젝트에서 고려할 점 |
|---|---|---|
| 현재 Isolation Forest + 대리 트리 | 이상 영역 설명에 적합 | 실제 THEN 기대값과 위반을 직접 만들지 못하므로 보조 분석으로 유지 |
| 자동 구간화 + OML Apriori / Python FP-Growth | 실제 값·구간의 연관규칙에 적합 | OML 입력 변환과 구간 매핑 또는 신규 Python 의존성이 필요하고 조합 수 제한이 중요 |
| 실제 컬럼별 얕은 예측 트리 | 조건 경로에서 실제 결과값·구간 발굴 가능 | 기존 sklearn·인코더·AST·SQL 평가를 가장 많이 재사용. 컬럼별 학습량 제한 필요 |

**권고: 현재 프로젝트의 첫 개선은 실제 컬럼별 예측 트리 기반 패턴 규칙으로 전환한다.** 현재 라이브러리 재사용과 자원 제한 구현의 용이성을 기준으로 한 판단이며, 실데이터 성능 측정 없이 모든 경우에 가장 빠르거나 많은 규칙을 찾는다고 단정하지 않는다. 트리는 연관규칙처럼 모든 겹치는 조건 조합을 열거하지 못하므로 이후 패턴 다양성을 비교해 Apriori/FP-Growth 보완 여부를 결정한다.

사용자가 범주형/연속형을 먼저 확정하는 단계는 추가하지 않는다. 기초통계·실제 자료형·고유값 수를 이용한 자동 전처리를 모델 내부에서 수행하고, 예외가 필요한 컬럼만 선택적으로 설정한다. NUMBER라고 무조건 연속값으로 처리하거나 숫자로 보이는 코드의 선행 0을 제거하지 않는다.

새 시나리오 내부 처리:

1. 원본 입수 → 공통 기초통계와 자동 전처리.
2. 대상 컬럼 Y를 순회하며 나머지 컬럼으로 Y의 실제 값/구간을 예측한다.
3. 트리 경로 A와 실제 결과 조건 B를 명시적으로 생성한다.
4. 별도 표본에서 지지 건수·신뢰도·향상도·안정성을 검증하고 중복/과도하게 복잡한 규칙을 줄인다.
5. 원본 전체에서 **A를 만족하고 B를 위배하는 행**을 찾는다.
6. 기존 최종 규칙 분석·검토 화면으로 전달한다.

예시(실제 데이터에서 발견했다는 의미가 아닌 출력 형식 예시):

```text
IF   성별코드 = 1 AND 학력코드 = 5
THEN 일시휴직여부 = 2

위반: IF 조건 충족 AND 실제 일시휴직여부가 2가 아님
표시: 결과 컬럼 / 기대값 2 / 실제값 / 위반 이유 / 규칙 신뢰도
```

```text
IF   업종코드 = 'A' AND 종사자수 >= 10
THEN 100 <= 매출액 < 500

위반: IF 조건 충족 AND 매출액이 기대 구간 밖
```

Isolation Forest를 사용한다면 위반 행의 검토 우선순위를 보조하는 용도로 둔다. 이상 점수를 실제 규칙 신뢰도로 사용하거나, 이상 판정 행을 학습에서 무조건 제외하지 않는다. 그 방식은 정상적인 희귀 패턴을 제거할 수 있다.

## 4. 공통 지표와 탐지 규약

A는 IF 조건, B는 실제 결과값/구간 조건이다. 동일한 평가 데이터와 NULL 정책을 사용한다.

| 공통 항목 | 정의 |
|---|---|
| 조건 해당 건수 | n(A) |
| 지지 건수 | n(A AND B) |
| 지지도 | n(A AND B) / 전체 평가 행 수 |
| 신뢰도 | n(A AND B) / n(A) |
| 향상도 | P(B given A) / P(B) |
| 위반 건수 | n(A AND B 불성립) |
| 검증 신뢰도 | 학습과 분리한 표본에서 같은 공식으로 계산 |
| 이상 점수 | 선택적인 별도 지표. 신뢰도·위반 확률과 혼용 금지 |

결과값이 NULL인 행을 위반으로 볼 것인지 명시해야 한다. 예를 들어 기대값 2에서 결측도 위반이라면 `Y IS NULL OR Y <> 2`를 평가한다. Oracle의 `NOT(Y = 2)`만으로는 NULL 행을 찾지 못한다. 표시용 텍스트를 다시 해석하지 말고, 안전한 조건/결과 AST를 SQL로 컴파일한다.

학습 표본, 규칙 선정용 검증 표본, 최종 시험 표본을 구분한다. 검증 지표를 보고 규칙을 선정했다면 그 수치를 독립적인 최종 시험 성능이라고 표시하지 않는다. 결과 컬럼과 그 원핫·결측·구간 파생변수는 IF 입력에서 모두 제외한다. 학습 전처리의 범주·결측값·구간 경계는 학습 표본에서만 정한다.

신뢰도 99%는 관측 비율이며 오류가 아닐 확률이나 통계적 확실성 99%가 아니다. 최소 조건 해당 건수, 검증 건수, 신뢰구간, 결과값 자체의 기본 빈도 대비 개선 여부를 함께 확인한다. 기준에 맞는 규칙이 없으면 규칙 0개를 정상 결과로 반환한다.

현재 선두 최대 25,000행 표본의 편향도 개선 대상이다. 선두 표본 내부에서 무작위 분할해도 원본 전체에 대한 대표성이 보장되지 않는다. 데이터 규모에 맞는 DB 표본 추출 또는 전체 순회 표본 방식을 비교하고, 대표성 제한을 기록한다.

## 5. DB·화면·최종 에디팅에서 재사용할 부분

- `INIT$_TB_RULEDISC_ASSOC_SUM`: 실제 결과 컬럼, 기대값, 조건/결과/전체 건수, 지지도·신뢰도·향상도가 있어 공통 규칙 결과로 재사용 가능하다. Python 결과의 논리적 모델 식별자를 사용하며 Oracle 물리 모델이 존재한다고 가정하는 메타데이터 조회는 분리한다.
- `INIT$_TB_RULEVIOL_ASSOC`: 결과 컬럼, 기대값, 실제값, 원본 행 식별자, 위반 이유를 재사용할 수 있다.
- 기존 XAI 실행/규칙 테이블: 이전 이상 설명 이력을 보존하고 모델 진단 용도로 유지할 수 있다. `ANOMALY_COUNT`, `RULE_PURITY`에 실제 패턴 지지 건수나 신뢰도를 다른 의미로 덮어쓰지 않는다.
- 범위/NULL/OR 규칙에는 버전·규칙 종류와 조건/결과 AST, 검증 근거를 명시적으로 저장해야 한다. 공통 결과 테이블의 JSON 컬럼 확장 또는 명시적 보조 저장 계약을 설계한다. 필요한 INIT SQL은 파일로 제공하고 사용자가 직접 실행한다.
- 화면은 IF, THEN, 실제 결과 컬럼, 지지도, 신뢰도, 향상도, 검증 건수, 전체 위반 수, 저장 행 수, 기대/실제값을 공통으로 표시한다. THEN의 등식·구간·결측 표현만 규칙 종류에 맞춰 렌더링한다.
- 전체 위반 행 수, 규칙×행 건수, 저장 미리보기 수를 분리한다. 특정 규칙이 앞 규칙의 저장 한도 때문에 조회되지 않도록 규칙별 배분 또는 원본 재조회 경로를 제공한다.

**테이블만 공통화하면 최종 적용에서 다시 실패한다.** 다음 두 경로가 단순 `COL = value AND ...` 문법을 가정하고 있어 반드시 함께 수정해야 한다.

1. `database/model_objects/INIT_MODEL_OBJECTS_20_RULE_MODELS.sql`의 `build_condition_where` 및 결과값 불일치 탐지.
2. `backend/services/edit_work_service.py`의 `_compile_discovered_association_expression` 및 발굴 규칙 적용 경로.

기존 수치 수식용 SYMBOLIC 테이블에는 회귀식·예측값·오차라는 의미가 있으므로 조건부 값 구간을 수식 모델로 위장하여 저장하지 않는다. 새 규칙의 AST를 M04002 조회뿐 아니라 최종 규칙 검토/에디팅 실행까지 같은 의미로 전달한다.

기존 XAI 실행은 이상 설명 결과로 보존한다. 현재 저장 이력에는 실제 THEN을 발굴한 학습 결과가 없으므로 새 버전으로 재실행해야 한다. 과거 모델 일치율을 신뢰도로 변환하는 마이그레이션은 하지 않는다. FLOW는 기존 시나리오와 분리한 혼합형 시나리오를 유지한다.

## 6. 합성 데이터 재현과 적용 완료 기준

재현 스크립트: `scripts/review_xai_rule_objective.py`. 운영 데이터·DB를 사용하지 않는다.

4,000행에 `GROUP_CODE -> RESULT_CODE` 패턴을 넣고 40행을 의도적으로 다르게 만들었다. 다른 숫자 컬럼에는 패턴 위반이 아닌 드문 큰 값을 추가했다. 전체 패턴 일치율은 99%이고, GROUP_CODE=0 조건에서는 98%, GROUP_CODE=1 조건에서는 100%다.

현재 환경에서의 실행 결과:

- 현재 이상 설명 규칙: 후보 100행 중 알려진 패턴 위반 40행, 해당 패턴을 지키는 행 60행.
- 실제 RESULT_CODE를 예측하는 간단한 트리: 기대값 0/1을 출력하고 불일치 40행을 구분.
- 간단한 트리의 전체 학습 정확도 약 98.93%, 별도 표본 정확도 99.2%. 이는 규칙별 신뢰도나 운영 데이터의 성능 수치가 아니다.

이 실험은 두 목표의 차이를 보여준다. 컬럼을 자동 선택하는 제품 엔진, 연속값 구간 선정, 규칙 필터·최종 시험 체계까지 구현하거나 실제 성능을 비교한 결과는 아니다.

변경 완료 기준:

1. 원본에 있는 결과 컬럼과 실제 기대값/구간이 모든 채택 규칙에 존재한다.
2. 결과 컬럼·파생변수가 IF 학습 입력에 들어가지 않는다.
3. 독립 집계한 신뢰도·향상도·조건 수·위반 수와 화면 값이 일치한다.
4. 정상적인 드문 행이라는 이유만으로 규칙 위반으로 저장하지 않는다.
5. NULL, 숫자 코드, 선행 0, 수치 구간 경계에서 Python·Oracle 판정이 같다.
6. 위반 행의 실제값을 저장하고, 제한된 미리보기 밖의 행도 해당 규칙으로 조회할 수 있다.
7. 퀵 에디팅 → M04002 → 최종 규칙 검토/적용까지 동일한 조건과 결과가 유지된다.
8. 규칙이 없는 결과, 과거 이상 설명 이력, 기존 시나리오가 정상 동작한다.

## 참고한 공식 문서

- [scikit-learn IsolationForest](https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.IsolationForest.html): 이상 탐지 점수와 임계값의 의미.
- [scikit-learn Decision Trees](https://scikit-learn.org/stable/modules/tree.html): 분류/회귀 트리, 해석과 복잡도 제한.
- [Oracle Association](https://www.oracle.com/goto/ml-association-rules): 실제 데이터 연관 패턴과 support/confidence/lift.
- [mlxtend association_rules](https://rasbt.github.io/mlxtend/user_guide/frequent_patterns/association_rules/): 조건/결과 및 지지도·신뢰도·향상도 정의.
